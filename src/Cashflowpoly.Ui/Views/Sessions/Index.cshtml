@* Fungsi file: Merender tampilan Razor untuk bagian Sessions/Index.cshtml beserta interaksi presentasinya.  Penjelasan rinci per baris. Nomor berikut mengacu kode asli setelah blok komentar ini (header Fungsi file dihitung sebagai baris 1). Komentar server-side tidak dikirim ke browser.
Baris sumber 2: Menetapkan model tampilan bertipe `Cashflowpoly.Ui.Models.SessionListViewModel`; Razor memeriksa akses properti Model saat kompilasi dan controller memasok instance model saat rendering.
Baris sumber 4: Membuka blok C# Razor untuk menghitung data tampilan di server sebelum markup halaman dikirim kepada browser.
Baris sumber 5: Memperbarui `ViewData[”Title”]` dengan mengambil teks terjemahan berkunci `nav.sessions` sesuai bahasa sesi pengguna.
Baris sumber 6: Menyimpan `isInstructor` dengan memeriksa peran instruktur pada sesi HTTP untuk menentukan kontrol tampilan yang tersedia.
Baris sumber 7: Menyimpan `totalSessions` dengan membaca nilai `Model.Items.Count` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya.
Baris sumber 8: Menyimpan `startedCount` dengan memanggil `Model.Items.Count(x → string.Equals(x.Status, ”STARTED”, StringComparison.OrdinalIgnoreCase))` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi. Nilai berupa fungsi; badan callback dijalankan hanya ketika fungsi dipanggil.
Baris sumber 9: Menyimpan `endedCount` dengan memanggil `Model.Items.Count(x → string.Equals(x.Status, ”ENDED”, StringComparison.OrdinalIgnoreCase))` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi. Nilai berupa fungsi; badan callback dijalankan hanya ketika fungsi dipanggil.
Baris sumber 10: Menyimpan `listTitle` dengan membaca nilai `isInstructor` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya.
Baris sumber 11: Menentukan hasil ketika kondisi ternary sebelumnya benar, yaitu `Context.T(”sessions.index.list_title.instructor”)`; nilai ini menjadi keluaran ekspresi pilihan.
Baris sumber 12: Menentukan hasil ketika kondisi ternary sebelumnya salah, yaitu `Context.T(”sessions.index.list_title.player”);`; nilai cadangan ini melengkapi ekspresi pilihan.
Baris sumber 13: Menyimpan `listSubtitle` dengan membaca nilai `isInstructor` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya.
Baris sumber 14: Menentukan hasil ketika kondisi ternary sebelumnya benar, yaitu `Context.T(”sessions.index.list_subtitle.instructor”)`; nilai ini menjadi keluaran ekspresi pilihan.
Baris sumber 15: Menentukan hasil ketika kondisi ternary sebelumnya salah, yaitu `Context.T(”sessions.index.list_subtitle.player”);`; nilai cadangan ini melengkapi ekspresi pilihan.
Baris sumber 16: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 18: Membentuk elemen `section` sebagai bagian halaman dengan topik konten tersendiri. Atribut `class` bernilai `section-shell w-full` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 19: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `page-intro` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 20: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `page-intro-main` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 21: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `eyebrow` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.management`, `sessions.player_management` menurut bahasa sesi.
Baris sumber 22: Membentuk elemen `h1` sebagai judul utama halaman. Atribut `class` bernilai `headline` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.title`, `sessions.player_title` menurut bahasa sesi.
Baris sumber 23: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `subhead` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 24: Menentukan hasil ketika kondisi ternary sebelumnya benar, yaitu `Context.T(”sessions.subtitle”)`; nilai ini menjadi keluaran ekspresi pilihan.
Baris sumber 25: Menentukan hasil ketika kondisi ternary sebelumnya salah, yaitu `Context.T(”sessions.player_subtitle”))‹/p›`; nilai cadangan ini melengkapi ekspresi pilihan.
Baris sumber 26: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 27: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `page-intro-actions` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 28: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Teks dinamis dibaca dari kunci terjemahan `sessions.live_log`, `sessions.readonly` menurut bahasa sesi.
Baris sumber 29: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 30: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 32: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `mt-6 ruleset-unified sessions-index-shell` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 33: Membentuk elemen `section` sebagai bagian halaman dengan topik konten tersendiri. Atribut `class` bernilai `sessions-index-section` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 34: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `ruleset-section-head` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 35: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `ruleset-section-title` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.index.summary_title` menurut bahasa sesi.
Baris sumber 36: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `ruleset-section-subtitle` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.index.summary_subtitle` menurut bahasa sesi.
Baris sumber 37: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 38: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `session-stats-grid mt-3` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 39: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `stat-card session-total-card` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 40: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `text-xs uppercase text-slate-500` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.total_sessions`, `sessions.my_sessions` menurut bahasa sesi.
Baris sumber 41: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `mt-2 text-2xl font-semibold text-slate-900` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 42: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 43: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `stat-card` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 44: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `text-xs uppercase text-slate-500` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.started` menurut bahasa sesi.
Baris sumber 45: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `mt-2 text-2xl font-semibold text-slate-900` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 46: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 47: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `stat-card` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 48: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `text-xs uppercase text-slate-500` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.ended` menurut bahasa sesi.
Baris sumber 49: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `mt-2 text-2xl font-semibold text-slate-900` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 50: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 51: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 52: Menutup elemen `section` (bagian halaman dengan topik konten tersendiri) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 54: Memeriksa kondisi `!string.IsNullOrWhiteSpace(Model.ErrorMessage)`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 55: Membuka ruang lingkup blok untuk `p`; deklarasi lokal dan urutan langkah di dalamnya dikelompokkan di sini.
Baris sumber 56: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `mt-4 alert alert-error` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 57: Menyisipkan hasil ekspresi Razor `@Model.ErrorMessage` ke HTML; nilai teks standar di-encode oleh Razor kecuali helper secara eksplisit menghasilkan HTML.
Baris sumber 58: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 59: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 61: Membentuk elemen `section` sebagai bagian halaman dengan topik konten tersendiri. Atribut `class` bernilai `sessions-index-section mt-4` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 62: Membentuk elemen `details` sebagai kontainer yang dapat diperluas dan diciutkan browser. Atribut `class` bernilai `mt-3 data-toggle` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 63: Membentuk elemen `summary` sebagai pemicu buka/tutup untuk elemen details. Teks dinamis dibaca dari kunci terjemahan `sessions.tips.instructor.title`, `sessions.tips.player.title` menurut bahasa sesi.
Baris sumber 64: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `mt-3 metric-panel` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 65: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `metric-list` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 66: Memeriksa kondisi `isInstructor`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 67: Membuka ruang lingkup blok untuk `div`; deklarasi lokal dan urutan langkah di dalamnya dikelompokkan di sini.
Baris sumber 68: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-value` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.step`, `sessions.tips.instructor.step1` menurut bahasa sesi.
Baris sumber 69: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-value` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.step`, `sessions.tips.instructor.step2` menurut bahasa sesi.
Baris sumber 70: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-value` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.step`, `sessions.tips.instructor.step3` menurut bahasa sesi.
Baris sumber 71: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 72: Memulai cabang alternatif yang hanya diproses apabila kondisi if sebelumnya tidak terpenuhi.
Baris sumber 73: Membuka ruang lingkup blok untuk `div`; deklarasi lokal dan urutan langkah di dalamnya dikelompokkan di sini.
Baris sumber 74: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-value` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.step`, `sessions.tips.player.step1` menurut bahasa sesi.
Baris sumber 75: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-value` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.step`, `sessions.tips.player.step2` menurut bahasa sesi.
Baris sumber 76: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-value` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.step`, `sessions.tips.player.step3` menurut bahasa sesi.
Baris sumber 77: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 78: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 79: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 80: Menutup elemen `details` (kontainer yang dapat diperluas dan diciutkan browser) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 82: Menutup elemen `section` (bagian halaman dengan topik konten tersendiri) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 84: Membentuk elemen `section` sebagai bagian halaman dengan topik konten tersendiri. Atribut `class` bernilai `sessions-index-section mt-5` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 85: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `ruleset-section-head` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 86: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `ruleset-section-title` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 87: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `ruleset-section-subtitle` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 88: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 89: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `mt-3 table-wrap` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 90: Membentuk elemen `table` sebagai tabel data dengan susunan baris dan kolom. Atribut `class` bernilai `w-full text-left text-sm mobile-card-table` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 91: Membentuk elemen `thead` sebagai kelompok header tabel. Atribut `class` bernilai `table-head` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 92: Membentuk elemen `tr` sebagai satu baris tabel.
Baris sumber 93: Membentuk elemen `th` sebagai sel header tabel. Atribut `class` bernilai `px-4 py-3` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `common.name` menurut bahasa sesi.
Baris sumber 94: Membentuk elemen `th` sebagai sel header tabel. Atribut `class` bernilai `px-4 py-3` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `common.mode` menurut bahasa sesi.
Baris sumber 95: Membentuk elemen `th` sebagai sel header tabel. Atribut `class` bernilai `px-4 py-3` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `common.status` menurut bahasa sesi.
Baris sumber 96: Membentuk elemen `th` sebagai sel header tabel. Atribut `class` bernilai `px-4 py-3` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `common.created` menurut bahasa sesi.
Baris sumber 97: Membentuk elemen `th` sebagai sel header tabel. Atribut `class` bernilai `px-4 py-3 text-right` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `common.action` menurut bahasa sesi.
Baris sumber 98: Menutup elemen `tr` (satu baris tabel) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 99: Menutup elemen `thead` (kelompok header tabel) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 100: Membentuk elemen `tbody` sebagai kelompok baris isi tabel.
Baris sumber 101: Memeriksa kondisi `Model.Items.Count ＝＝ 0`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 102: Membuka ruang lingkup blok untuk `tbody`; deklarasi lokal dan urutan langkah di dalamnya dikelompokkan di sini.
Baris sumber 103: Membentuk elemen `tr` sebagai satu baris tabel.
Baris sumber 104: Membentuk elemen `td` sebagai sel nilai dalam tabel. Atribut `class` bernilai `px-4 py-4 text-slate-500 mobile-card-empty` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `state.no_sessions` menurut bahasa sesi.
Baris sumber 105: Menutup elemen `tr` (satu baris tabel) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 106: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 107: Memulai cabang alternatif yang hanya diproses apabila kondisi if sebelumnya tidak terpenuhi.
Baris sumber 108: Membuka ruang lingkup blok untuk `td`; deklarasi lokal dan urutan langkah di dalamnya dikelompokkan di sini.
Baris sumber 109: Mengiterasi `var item in Model.Items`; setiap anggota koleksi mendapat pemrosesan atau markup yang sama dengan nilai item masing-masing.
Baris sumber 110: Membuka ruang lingkup blok untuk `td`; deklarasi lokal dan urutan langkah di dalamnya dikelompokkan di sini.
Baris sumber 111: Menyimpan `modeLabel` dengan memanggil `Context.TRulesetMode(item.Mode)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 112: Membentuk elemen `tr` sebagai satu baris tabel. Atribut `class` bernilai `border-t border-slate-200` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 113: Membentuk elemen `td` sebagai sel nilai dalam tabel. Atribut `class` bernilai `px-4 py-3 font-medium text-slate-900` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `common.name` menurut bahasa sesi.
Baris sumber 114: Membentuk elemen `td` sebagai sel nilai dalam tabel. Atribut `class` bernilai `px-4 py-3 text-slate-600` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `common.mode` menurut bahasa sesi.
Baris sumber 115: Membentuk elemen `td` sebagai sel nilai dalam tabel. Atribut `class` bernilai `px-4 py-3 text-slate-600` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `common.status` menurut bahasa sesi.
Baris sumber 116: Membuka blok C# Razor untuk menghitung data tampilan di server sebelum markup halaman dikirim kepada browser.
Baris sumber 117: Menyimpan `statusClass` dengan memanggil `string.Equals(item.Status, ”STARTED”, StringComparison.OrdinalIgnoreCase)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 118: Menentukan hasil ketika kondisi ternary sebelumnya benar, yaitu `”status-active”`; nilai ini menjadi keluaran ekspresi pilihan.
Baris sumber 119: Menentukan hasil ketika kondisi ternary sebelumnya salah, yaitu `string.Equals(item.Status, ”CREATED”, StringComparison.OrdinalIgnoreCase)`; nilai cadangan ini melengkapi ekspresi pilihan.
Baris sumber 120: Menentukan hasil ketika kondisi ternary sebelumnya benar, yaitu `”status-draft”`; nilai ini menjadi keluaran ekspresi pilihan.
Baris sumber 121: Menentukan hasil ketika kondisi ternary sebelumnya salah, yaitu `”status-retired”;`; nilai cadangan ini melengkapi ekspresi pilihan.
Baris sumber 122: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 123: Membentuk elemen `span` sebagai wadah teks inline tanpa memulai blok baru.
Baris sumber 124: Menutup elemen `td` (sel nilai dalam tabel) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 125: Membentuk elemen `td` sebagai sel nilai dalam tabel. Atribut `class` bernilai `px-4 py-3 text-slate-600` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `common.created` menurut bahasa sesi.
Baris sumber 126: Membentuk elemen `td` sebagai sel nilai dalam tabel. Atribut `class` bernilai `px-4 py-3 text-right` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `common.action` menurut bahasa sesi.
Baris sumber 127: Membentuk elemen `a` sebagai tautan navigasi ke halaman atau sasaran lain. Atribut `class` bernilai `table-link` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Isi `‹a class＝”table-link”` menjadi teks yang terlihat pada komponen.
Baris sumber 127: Baris sumber 128: Melengkapi atribut `asp-controller` pada `a` dengan `”Sessions” asp-action＝”Details” asp-route-sessionId＝”@item.SessionId”›@(isInstructor ? Context.T(”common.detail”) : Context.T(”sessions.view_progress”))‹/a›`; nama controller tujuan yang dipakai tag helper pembuat URL.
Baris sumber 129: Menutup elemen `td` (sel nilai dalam tabel) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 130: Menutup elemen `tr` (satu baris tabel) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 131: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 132: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 133: Menutup elemen `tbody` (kelompok baris isi tabel) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 134: Menutup elemen `table` (tabel data dengan susunan baris dan kolom) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 135: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 136: Menutup elemen `section` (bagian halaman dengan topik konten tersendiri) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 137: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 138: Menutup elemen `section` (bagian halaman dengan topik konten tersendiri) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
*@
@model Cashflowpoly.Ui.Models.SessionListViewModel

@{
    ViewData["Title"] = Context.T("nav.sessions");
    var isInstructor = Context.IsInstructor();
    var currentPlayerId = Context.User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value;
    var totalSessions = Model.Items.Count;
    var startedCount = Model.Items.Count(x => string.Equals(x.Status, "STARTED", StringComparison.OrdinalIgnoreCase));
    var endedCount = Model.Items.Count(x => string.Equals(x.Status, "ENDED", StringComparison.OrdinalIgnoreCase));
    var listTitle = isInstructor
        ? Context.T("sessions.index.list_title.instructor")
        : Context.T("sessions.index.list_title.player");
    var listSubtitle = isInstructor
        ? Context.T("sessions.index.list_subtitle.instructor")
        : Context.T("sessions.index.list_subtitle.player");
}

<section class="section-shell w-full">
    <div class="page-intro">
        <div class="page-intro-main">
            <p class="eyebrow">@(isInstructor ? Context.T("sessions.management") : Context.T("sessions.player_management"))</p>
            <h1 class="headline">@(isInstructor ? Context.T("sessions.title") : Context.T("sessions.player_title"))</h1>
            <p class="subhead">@(isInstructor
                ? Context.T("sessions.subtitle")
                : Context.T("sessions.player_subtitle"))</p>
        </div>
        <div class="page-intro-actions">
            <div class="badge @(isInstructor ? "badge-amber" : "badge-emerald")">@(isInstructor ? Context.T("sessions.live_log") : Context.T("sessions.readonly"))</div>
        </div>
    </div>

    <div class="mt-6 ruleset-unified sessions-index-shell">
        <section class="sessions-index-section">
            <div class="ruleset-section-head">
                <p class="ruleset-section-title">@Context.T("sessions.index.summary_title")</p>
                <p class="ruleset-section-subtitle">@Context.T("sessions.index.summary_subtitle")</p>
            </div>
            <div class="session-stats-grid mt-3">
                <div class="stat-card session-total-card">
                    <p class="text-xs uppercase text-slate-500">@(isInstructor ? Context.T("sessions.total_sessions") : Context.T("sessions.my_sessions"))</p>
                    <p class="mt-2 text-2xl font-semibold text-slate-900">@(Model.SessionsAvailable ? totalSessions.ToString() : "—")</p>
                </div>
                <div class="stat-card">
                    <p class="text-xs uppercase text-slate-500">@Context.T("sessions.started")</p>
                    <p class="mt-2 text-2xl font-semibold text-slate-900">@(Model.SessionsAvailable ? startedCount.ToString() : "—")</p>
                </div>
                <div class="stat-card">
                    <p class="text-xs uppercase text-slate-500">@Context.T("sessions.ended")</p>
                    <p class="mt-2 text-2xl font-semibold text-slate-900">@(Model.SessionsAvailable ? endedCount.ToString() : "—")</p>
                </div>
                <div class="stat-card">
                    <p class="text-xs uppercase text-slate-500">@Context.T("players.monitored_players")</p>
                    <p class="mt-2 text-2xl font-semibold text-slate-900">@(Model.MonitoredPlayers?.ToString() ?? "—")</p>
                </div>
            </div>
        </section>

        @if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
        {
            <div class="mt-4 alert alert-error" aria-live="polite">
                @Model.ErrorMessage
            </div>
        }

        <section class="sessions-index-section mt-4">
            <details class="mt-3 data-toggle">
                <summary>@(isInstructor ? Context.T("sessions.tips.instructor.title") : Context.T("sessions.tips.player.title"))</summary>
                <div class="mt-3 metric-panel">
                    <div class="metric-list">
                        @if (isInstructor)
                        {
                            <div class="metric-row"><span class="metric-key">@Context.T("sessions.step") 1</span><span class="metric-value">@Context.T("sessions.tips.instructor.step1")</span></div>
                            <div class="metric-row"><span class="metric-key">@Context.T("sessions.step") 2</span><span class="metric-value">@Context.T("sessions.tips.instructor.step2")</span></div>
                            <div class="metric-row"><span class="metric-key">@Context.T("sessions.step") 3</span><span class="metric-value">@Context.T("sessions.tips.instructor.step3")</span></div>
                        }
                        else
                        {
                            <div class="metric-row"><span class="metric-key">@Context.T("sessions.step") 1</span><span class="metric-value">@Context.T("sessions.tips.player.step1")</span></div>
                            <div class="metric-row"><span class="metric-key">@Context.T("sessions.step") 2</span><span class="metric-value">@Context.T("sessions.tips.player.step2")</span></div>
                            <div class="metric-row"><span class="metric-key">@Context.T("sessions.step") 3</span><span class="metric-value">@Context.T("sessions.tips.player.step3")</span></div>
                        }
                    </div>
                </div>
            </details>

        </section>

        <section class="sessions-index-section mt-5">
            <div class="ruleset-section-head">
                <p class="ruleset-section-title">@listTitle</p>
                <p class="ruleset-section-subtitle">@listSubtitle</p>
            </div>
            <div class="mt-3 players-session-stack">
                @if (Model.Items.Count == 0 && Model.SessionsAvailable)
                {
                    <p class="text-slate-500">@Context.T("state.no_sessions")</p>
                }
                @foreach (var session in Model.SessionGroups)
                {
                    var statusClass = session.Status == "STARTED" ? "status-active" : session.Status == "CREATED" ? "status-draft" : "status-retired";
                    <details class="data-toggle players-session-card" open>
                        <summary class="players-session-summary">
                            <span class="players-session-summary-main">
                                <span class="font-semibold text-slate-900">@session.SessionName</span>
                                <span class="text-xs text-slate-500">(@(session.ParticipantsAvailable ? session.Players.Count.ToString() : "—") @Context.T("players.count_suffix"))</span>
                                <span class="hint-chip">@Context.TRulesetMode(session.Mode)</span>
                                <span class="status-pill @statusClass">@Context.TSessionStatus(session.Status)</span>
                            </span>
                        </summary>
                        <div class="players-session-meta mt-3">
                            <span class="hint-chip">@Context.T("common.created"): @session.CreatedAt.ToLocalTime().ToString("dd/MM/yyyy HH:mm")</span>
                            @if (session.StartedAt.HasValue)
                            {
                                <span class="hint-chip">@Context.T("common.start"): @session.StartedAt.Value.ToLocalTime().ToString("dd/MM/yyyy HH:mm")</span>
                            }
                            @if (session.EndedAt.HasValue)
                            {
                                <span class="hint-chip">@Context.T("common.finish"): @session.EndedAt.Value.ToLocalTime().ToString("dd/MM/yyyy HH:mm")</span>
                            }
                            <a class="btn-ghost players-session-detail" asp-controller="Sessions" asp-action="Details" asp-route-sessionId="@session.SessionId">@Context.T("common.detail")</a>
                        </div>
                        @if (!session.ParticipantsAvailable)
                        {
                            <p class="alert alert-info mt-3">@Context.T("players.error.load_session_details_partial")</p>
                        }
                        else if (session.Players.Count == 0)
                        {
                            <p class="mt-3 text-slate-500">@Context.T("state.no_players")</p>
                        }
                        else
                        {
                            @if (session.Status != "ENDED" || !session.ResultsAvailable)
                            {
                                <p class="mt-3 text-sm text-slate-500">@Context.T("sessions.results_pending")</p>
                            }
                            <div class="mt-3 table-wrap">
                                <table class="w-full text-left text-sm mobile-card-table players-session-table">
                                    <colgroup><col class="players-session-col-order" /><col class="players-session-col-rank" /><col class="players-session-col-name" /><col class="players-session-col-score" /><col class="players-session-col-action" /></colgroup>
                                    <thead class="table-head"><tr>
                                        <th class="px-4 py-3">@Context.T("common.turn_order")</th>
                                        <th class="px-4 py-3">@Context.T("common.rank")</th>
                                        <th class="px-4 py-3 players-session-name-heading">@Context.T("common.name")</th>
                                        <th class="px-4 py-3">@Context.T("players.index.happiness_points")</th>
                                        <th class="px-4 py-3">@Context.T("players.index.analytics_column")</th>
                                    </tr></thead>
                                    <tbody>
                                    @foreach (var player in session.Players)
                                    {
                                        <tr class="border-t border-slate-200 @(player.FinalRank == 1 ? "winner-row" : "")">
                                            <td class="px-4 py-3 players-session-number" data-label="@Context.T("common.turn_order")">@(player.PlayerOrder > 0 ? player.PlayerOrder.ToString() : "—")</td>
                                            <td class="px-4 py-3 players-session-number" data-label="@Context.T("common.rank")"><span class="players-session-rank rank-@player.FinalRank">@(player.FinalRank > 0 ? player.FinalRank.ToString() : "—")</span></td>
                                            <td class="px-4 py-3 players-session-player" data-label="@Context.T("common.name")">
                                                <div class="flex items-center gap-3">
                                                    <img class="h-7 w-7 rounded-full border border-slate-200 object-cover" src="/images/players/@(Math.Max(0, player.PlayerOrder - 1) % 4 + 1).png" alt="" />
                                                    <span class="font-medium text-slate-900">@player.DisplayName</span>
                                                </div>
                                            </td>
                                            <td class="px-4 py-3 players-session-number" data-label="@Context.T("players.index.happiness_points")"><strong class="players-session-score">@(player.HappinessPointsTotal?.ToString("0.##") ?? "—")</strong></td>
                                            <td class="px-4 py-3 players-session-action" data-label="@Context.T("players.index.analytics_column")">
                                                @if (session.Status != "CREATED" && (isInstructor || player.PlayerId.ToString() == currentPlayerId))
                                                {
                                                    <a class="table-link" asp-controller="Players" asp-action="Details" asp-route-sessionId="@session.SessionId" asp-route-playerId="@player.PlayerId">@Context.T("players.index.view_detail")</a>
                                                }
                                                else
                                                {
                                                    <span class="text-slate-400" aria-label="@Context.T("players.index.detail_unavailable")">—</span>
                                                }
                                            </td>
                                        </tr>
                                    }
                                    </tbody>
                                </table>
                            </div>
                        }
                    </details>
                }
            </div>
        </section>
    </div>
</section>
