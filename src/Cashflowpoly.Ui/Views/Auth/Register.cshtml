@* Fungsi file: Merender tampilan Razor untuk bagian Auth/Register.cshtml beserta interaksi presentasinya.  Penjelasan rinci per baris. Nomor berikut mengacu kode asli setelah blok komentar ini (header Fungsi file dihitung sebagai baris 1). Komentar server-side tidak dikirim ke browser.
Baris sumber 2: Menetapkan model tampilan bertipe `Cashflowpoly.Ui.Models.RegisterViewModel`; Razor memeriksa akses properti Model saat kompilasi dan controller memasok instance model saat rendering.
Baris sumber 3: Mengimpor namespace `Microsoft.AspNetCore.Http` agar tipe di dalamnya dapat dipakai tanpa nama lengkap.
Baris sumber 5: Membuka blok C# Razor untuk menghitung data tampilan di server sebelum markup halaman dikirim kepada browser.
Baris sumber 6: Memperbarui `ViewData[”Title”]` dengan mengambil teks terjemahan berkunci `auth.register_title` sesuai bahasa sesi pengguna.
Baris sumber 7: Memperbarui `ViewData[”MetaDescription”]` dengan mengambil teks terjemahan berkunci `register.meta_desc` sesuai bahasa sesi pengguna.
Baris sumber 8: Memperbarui `ViewData[”MetaKeywords”]` dengan mengambil teks terjemahan berkunci `register.meta_keywords` sesuai bahasa sesi pengguna.
Baris sumber 9: Memperbarui `ViewData[”HideFooter”]` dengan mengaktifkan flag Boolean dengan nilai true.
Baris sumber 10: Memperbarui `ViewData[”HideHeader”]` dengan mengaktifkan flag Boolean dengan nilai true.
Baris sumber 11: Menyimpan `activeLanguage` dengan memanggil `UiText.NormalizeLanguage(Context.Session.GetString(AuthConstants.SessionLanguageKey))` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 12: Menyimpan `isEnglish` dengan memanggil `string.Equals(activeLanguage, AuthConstants.LanguageEn, StringComparison.OrdinalIgnoreCase)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 13: Menyimpan `returnUrl` dengan membaca nilai `$”｛Context.Request.Path｝｛Context.Request.QueryString｝”` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya.
Baris sumber 14: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 16: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `auth-split-shell` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 17: Membentuk elemen `section` sebagai bagian halaman dengan topik konten tersendiri. Atribut `class` bernilai `auth-split-card auth-mode-register` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 18: Membentuk elemen `aside` sebagai informasi pendamping konten utama. Atribut `class` bernilai `auth-split-right` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 19: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `auth-panel-lang-corner` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 20: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `auth-panel-lang` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `role` bernilai `group` menentukan peran semantik yang diumumkan kepada teknologi bantu. Teks dinamis dibaca dari kunci terjemahan `lang.label` menurut bahasa sesi.
Baris sumber 21: Membentuk elemen `form` sebagai kelompok kontrol masukan yang dikirim ke endpoint server. Atribut `method` bernilai `post` menentukan metode HTTP ketika formulir dikirim. Atribut `asp-controller` bernilai `Language` menentukan nama controller tujuan yang dipakai tag helper pembuat URL. Atribut `asp-action` bernilai `Set` menentukan nama action tujuan yang dipakai tag helper pembuat URL. Ketika dikirim, nama dan nilai kontrol di dalamnya digunakan model binding pada server.
Baris sumber 22: Menghasilkan bagian tampilan melalui `@Html.AntiForgeryToken()`; Razor menjalankan helper di server dan menyisipkan hasil pada posisi markup ini.
Baris sumber 23: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `type` bernilai `hidden` menentukan jenis kontrol atau tipe konten. Atribut `name` bernilai `language` menentukan nama field yang dikirim atau nama metadata.
Baris sumber 24: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `type` bernilai `hidden` menentukan jenis kontrol atau tipe konten. Atribut `name` bernilai `returnUrl` menentukan nama field yang dikirim atau nama metadata.
Baris sumber 25: Membentuk elemen `button` sebagai kontrol interaksi yang dapat diaktifkan pengguna. Atribut `type` bernilai `submit` menentukan jenis kontrol atau tipe konten. Teks dinamis dibaca dari kunci terjemahan `lang.code.id` menurut bahasa sesi.
Baris sumber 26: Menutup elemen `form` (kelompok kontrol masukan yang dikirim ke endpoint server) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 27: Membentuk elemen `form` sebagai kelompok kontrol masukan yang dikirim ke endpoint server. Atribut `method` bernilai `post` menentukan metode HTTP ketika formulir dikirim. Atribut `asp-controller` bernilai `Language` menentukan nama controller tujuan yang dipakai tag helper pembuat URL. Atribut `asp-action` bernilai `Set` menentukan nama action tujuan yang dipakai tag helper pembuat URL. Ketika dikirim, nama dan nilai kontrol di dalamnya digunakan model binding pada server.
Baris sumber 28: Menghasilkan bagian tampilan melalui `@Html.AntiForgeryToken()`; Razor menjalankan helper di server dan menyisipkan hasil pada posisi markup ini.
Baris sumber 29: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `type` bernilai `hidden` menentukan jenis kontrol atau tipe konten. Atribut `name` bernilai `language` menentukan nama field yang dikirim atau nama metadata.
Baris sumber 30: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `type` bernilai `hidden` menentukan jenis kontrol atau tipe konten. Atribut `name` bernilai `returnUrl` menentukan nama field yang dikirim atau nama metadata.
Baris sumber 31: Membentuk elemen `button` sebagai kontrol interaksi yang dapat diaktifkan pengguna. Atribut `type` bernilai `submit` menentukan jenis kontrol atau tipe konten. Teks dinamis dibaca dari kunci terjemahan `lang.code.en` menurut bahasa sesi.
Baris sumber 32: Menutup elemen `form` (kelompok kontrol masukan yang dikirim ke endpoint server) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 33: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 34: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 35: Membentuk elemen `a` sebagai tautan navigasi ke halaman atau sasaran lain. Atribut `class` bernilai `auth-panel-brand` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `asp-controller` bernilai `Home` menentukan nama controller tujuan yang dipakai tag helper pembuat URL. Atribut `asp-action` bernilai `Index` menentukan nama action tujuan yang dipakai tag helper pembuat URL.
Baris sumber 36: Menyediakan elemen `img` sebagai gambar dengan sumber dan teks alternatif. Atribut `class` bernilai `auth-panel-brand-mark` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `src` bernilai `~/images/images.jpg` menentukan lokasi sumber aset. Atribut `asp-append-version` bernilai `true` menentukan izin tag helper menambahkan versi/hash aset agar cache diperbarui ketika berkas berubah. Teks dinamis dibaca dari kunci terjemahan `layout.logo_alt` menurut bahasa sesi.
Baris sumber 37: Membentuk elemen `span` sebagai wadah teks inline tanpa memulai blok baru. Atribut `class` bernilai `auth-panel-brand-text` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `layout.brand_name` menurut bahasa sesi.
Baris sumber 38: Menutup elemen `a` (tautan navigasi ke halaman atau sasaran lain) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 39: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `auth-panel-content` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 40: Membentuk elemen `h2` sebagai judul bagian tingkat dua. Teks dinamis dibaca dari kunci terjemahan `auth.login_panel_title` menurut bahasa sesi.
Baris sumber 41: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Teks dinamis dibaca dari kunci terjemahan `auth.login_panel_desc` menurut bahasa sesi.
Baris sumber 42: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 43: Membentuk elemen `a` sebagai tautan navigasi ke halaman atau sasaran lain. Atribut `class` bernilai `auth-signup-cta js-auth-switch` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `asp-controller` bernilai `Auth` menentukan nama controller tujuan yang dipakai tag helper pembuat URL. Atribut `asp-action` bernilai `Login` menentukan nama action tujuan yang dipakai tag helper pembuat URL. Teks dinamis dibaca dari kunci terjemahan `auth.login_panel_cta` menurut bahasa sesi. Nilai `@Model.ReturnUrl` berasal dari model yang disusun controller. Atribut asp-route-* memasok parameter route sehingga URL menunjuk data sesi, pemain, atau ruleset yang sesuai.
Baris sumber 44: Menutup elemen `aside` (informasi pendamping konten utama) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 46: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `auth-split-left` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 47: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `auth-content-head` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 48: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `auth-kicker` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `auth.access` menurut bahasa sesi.
Baris sumber 49: Membentuk elemen `h1` sebagai judul utama halaman. Atribut `class` bernilai `auth-heading` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `id` bernilai `auth-register-heading` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Teks dinamis dibaca dari kunci terjemahan `auth.register_title` menurut bahasa sesi.
Baris sumber 50: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `auth-copy` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `auth.register_subtitle` menurut bahasa sesi.
Baris sumber 51: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 53: Membentuk elemen `form` sebagai kelompok kontrol masukan yang dikirim ke endpoint server. Atribut `class` bernilai `auth-form` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `method` bernilai `post` menentukan metode HTTP ketika formulir dikirim. Atribut `asp-controller` bernilai `Auth` menentukan nama controller tujuan yang dipakai tag helper pembuat URL. Atribut `asp-action` bernilai `Register` menentukan nama action tujuan yang dipakai tag helper pembuat URL. Ketika dikirim, nama dan nilai kontrol di dalamnya digunakan model binding pada server.
Baris sumber 54: Menghasilkan bagian tampilan melalui `@Html.AntiForgeryToken()`; Razor menjalankan helper di server dan menyisipkan hasil pada posisi markup ini.
Baris sumber 55: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `type` bernilai `hidden` menentukan jenis kontrol atau tipe konten. Atribut `asp-for` bernilai `ReturnUrl` menentukan properti model yang diikat ke kontrol formulir.
Baris sumber 57: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `auth-form-grid auth-form-grid-2` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 58: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `form-field` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 59: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `auth-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `auth.display_name` menurut bahasa sesi.
Baris sumber 60: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `class` bernilai `auth-input` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Isi `‹input class＝”auth-input”` menjadi teks yang terlihat pada komponen.
Baris sumber 60: Baris sumber 61: Melengkapi atribut `id` pada `input` dengan `”register-display-name”`; identitas unik elemen untuk selector, label, atau sasaran tautan.
Baris sumber 60: Baris sumber 62: Melengkapi atribut `asp-for` pada `input` dengan `”DisplayName”`; properti model yang diikat ke kontrol formulir.
Baris sumber 60: Baris sumber 64: Melengkapi atribut `required` pada `input` dengan ``; kewajiban pengisian kontrol pada validasi browser.
Baris sumber 60: Baris sumber 65: Melengkapi atribut `placeholder` pada `input` dengan `”@Context.T(”auth.placeholder.full_name”)” /›`; contoh atau petunjuk masukan sebelum pengguna mengetik.
Baris sumber 63: Memperbarui `autocomplete` dengan menggunakan literal `”name”` sebagai teks, kunci, warna, atau isi template sesuai tempat pemakaiannya.
Baris sumber 66: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 68: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `form-field` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 69: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `auth-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `auth.username` menurut bahasa sesi.
Baris sumber 70: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `class` bernilai `auth-input` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Isi `‹input class＝”auth-input”` menjadi teks yang terlihat pada komponen.
Baris sumber 70: Baris sumber 71: Melengkapi atribut `id` pada `input` dengan `”register-username”`; identitas unik elemen untuk selector, label, atau sasaran tautan.
Baris sumber 70: Baris sumber 72: Melengkapi atribut `asp-for` pada `input` dengan `”Username”`; properti model yang diikat ke kontrol formulir.
Baris sumber 70: Baris sumber 79: Melengkapi atribut `required` pada `input` dengan ``; kewajiban pengisian kontrol pada validasi browser.
Baris sumber 70: Baris sumber 80: Melengkapi atribut `placeholder` pada `input` dengan `”@Context.T(”auth.placeholder.new_username”)” /›`; contoh atau petunjuk masukan sebelum pengguna mengetik.
Baris sumber 73: Memperbarui `autocomplete` dengan menggunakan literal `”username”` sebagai teks, kunci, warna, atau isi template sesuai tempat pemakaiannya.
Baris sumber 74: Memperbarui `autocapitalize` dengan menggunakan literal `”none”` sebagai teks, kunci, warna, atau isi template sesuai tempat pemakaiannya.
Baris sumber 75: Memperbarui `autocorrect` dengan menggunakan literal `”off”` sebagai teks, kunci, warna, atau isi template sesuai tempat pemakaiannya.
Baris sumber 76: Memperbarui `spellcheck` dengan menggunakan literal `”false”` sebagai teks, kunci, warna, atau isi template sesuai tempat pemakaiannya.
Baris sumber 77: Memperbarui `inputmode` dengan menggunakan literal `”text”` sebagai teks, kunci, warna, atau isi template sesuai tempat pemakaiannya.
Baris sumber 78: Memperbarui `enterkeyhint` dengan menggunakan literal `”next”` sebagai teks, kunci, warna, atau isi template sesuai tempat pemakaiannya.
Baris sumber 81: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 82: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 84: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `auth-form-grid auth-form-grid-2` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 85: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `form-field` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 86: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `auth-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `auth.password` menurut bahasa sesi.
Baris sumber 87: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `class` bernilai `auth-input` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Isi `‹input class＝”auth-input”` menjadi teks yang terlihat pada komponen.
Baris sumber 87: Baris sumber 88: Melengkapi atribut `id` pada `input` dengan `”register-password”`; identitas unik elemen untuk selector, label, atau sasaran tautan.
Baris sumber 87: Baris sumber 89: Melengkapi atribut `asp-for` pada `input` dengan `”Password”`; properti model yang diikat ke kontrol formulir.
Baris sumber 87: Baris sumber 90: Melengkapi atribut `type` pada `input` dengan `”password”`; jenis kontrol atau tipe konten.
Baris sumber 87: Baris sumber 93: Melengkapi atribut `required` pada `input` dengan ``; kewajiban pengisian kontrol pada validasi browser.
Baris sumber 87: Baris sumber 94: Melengkapi atribut `placeholder` pada `input` dengan `”********” /›`; contoh atau petunjuk masukan sebelum pengguna mengetik.
Baris sumber 91: Memperbarui `autocomplete` dengan menggunakan literal `”new-password”` sebagai teks, kunci, warna, atau isi template sesuai tempat pemakaiannya.
Baris sumber 92: Memperbarui `enterkeyhint` dengan menggunakan literal `”next”` sebagai teks, kunci, warna, atau isi template sesuai tempat pemakaiannya.
Baris sumber 95: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 97: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `form-field` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 98: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `auth-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `auth.confirm_password` menurut bahasa sesi.
Baris sumber 99: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `class` bernilai `auth-input` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Isi `‹input class＝”auth-input”` menjadi teks yang terlihat pada komponen.
Baris sumber 99: Baris sumber 100: Melengkapi atribut `id` pada `input` dengan `”register-confirm-password”`; identitas unik elemen untuk selector, label, atau sasaran tautan.
Baris sumber 99: Baris sumber 101: Melengkapi atribut `asp-for` pada `input` dengan `”ConfirmPassword”`; properti model yang diikat ke kontrol formulir.
Baris sumber 99: Baris sumber 102: Melengkapi atribut `type` pada `input` dengan `”password”`; jenis kontrol atau tipe konten.
Baris sumber 99: Baris sumber 105: Melengkapi atribut `required` pada `input` dengan ``; kewajiban pengisian kontrol pada validasi browser.
Baris sumber 99: Baris sumber 106: Melengkapi atribut `placeholder` pada `input` dengan `”********” /›`; contoh atau petunjuk masukan sebelum pengguna mengetik.
Baris sumber 103: Memperbarui `autocomplete` dengan menggunakan literal `”new-password”` sebagai teks, kunci, warna, atau isi template sesuai tempat pemakaiannya.
Baris sumber 104: Memperbarui `enterkeyhint` dengan menggunakan literal `”done”` sebagai teks, kunci, warna, atau isi template sesuai tempat pemakaiannya.
Baris sumber 107: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 108: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 110: Memeriksa kondisi `!string.IsNullOrWhiteSpace(Model.ErrorMessage)`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 111: Membuka ruang lingkup blok untuk `input`; deklarasi lokal dan urutan langkah di dalamnya dikelompokkan di sini.
Baris sumber 112: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `alert alert-error` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 113: Menyisipkan hasil ekspresi Razor `@Model.ErrorMessage` ke HTML; nilai teks standar di-encode oleh Razor kecuali helper secara eksplisit menghasilkan HTML.
Baris sumber 114: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 115: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 117: Membentuk elemen `button` sebagai kontrol interaksi yang dapat diaktifkan pengguna. Atribut `class` bernilai `auth-login-btn` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `type` bernilai `submit` menentukan jenis kontrol atau tipe konten. Teks dinamis dibaca dari kunci terjemahan `auth.submit_register` menurut bahasa sesi.
Baris sumber 118: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `auth-register-copy` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 119: Menyisipkan hasil ekspresi Razor `@Context.T(”auth.have_account”)` ke HTML; nilai teks standar di-encode oleh Razor kecuali helper secara eksplisit menghasilkan HTML.
Baris sumber 120: Membentuk elemen `a` sebagai tautan navigasi ke halaman atau sasaran lain. Atribut `class` bernilai `js-auth-switch` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `asp-controller` bernilai `Auth` menentukan nama controller tujuan yang dipakai tag helper pembuat URL. Atribut `asp-action` bernilai `Login` menentukan nama action tujuan yang dipakai tag helper pembuat URL. Teks dinamis dibaca dari kunci terjemahan `auth.login` menurut bahasa sesi. Nilai `@Model.ReturnUrl` berasal dari model yang disusun controller. Atribut asp-route-* memasok parameter route sehingga URL menunjuk data sesi, pemain, atau ruleset yang sesuai.
Baris sumber 121: Menutup elemen `p` (paragraf atau label penjelasan) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 122: Menutup elemen `form` (kelompok kontrol masukan yang dikirim ke endpoint server) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 123: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 124: Menutup elemen `section` (bagian halaman dengan topik konten tersendiri) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 125: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
*@
@model Cashflowpoly.Ui.Models.RegisterViewModel
@using Microsoft.AspNetCore.Http

@{
    ViewData["Title"] = Context.T("auth.register_title");
    ViewData["MetaDescription"] = Context.T("register.meta_desc");
    ViewData["MetaKeywords"] = Context.T("register.meta_keywords");
    ViewData["HideFooter"] = true;
    ViewData["HideHeader"] = true;
    var activeLanguage = UiText.NormalizeLanguage(Context.Session.GetString(AuthConstants.SessionLanguageKey));
    var isEnglish = string.Equals(activeLanguage, AuthConstants.LanguageEn, StringComparison.OrdinalIgnoreCase);
    var returnUrl = $"{Context.Request.Path}{Context.Request.QueryString}";
}

<div class="auth-split-shell">
    <section class="auth-split-card auth-mode-register" aria-labelledby="auth-register-heading">
        <aside class="auth-split-right">
            <div class="auth-panel-lang-corner">
                <div class="auth-panel-lang" role="group" aria-label="@Context.T("lang.label")">
                    <form method="post" asp-controller="Language" asp-action="Set">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="language" value="@AuthConstants.LanguageId" />
                        <input type="hidden" name="returnUrl" value="@returnUrl" />
                        <button class="auth-panel-lang-btn @(isEnglish ? string.Empty : "active")" type="submit">@Context.T("lang.code.id")</button>
                    </form>
                    <form method="post" asp-controller="Language" asp-action="Set">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="language" value="@AuthConstants.LanguageEn" />
                        <input type="hidden" name="returnUrl" value="@returnUrl" />
                        <button class="auth-panel-lang-btn @(isEnglish ? "active" : string.Empty)" type="submit">@Context.T("lang.code.en")</button>
                    </form>
                </div>
            </div>
            <a class="auth-panel-brand" asp-area="" asp-controller="Home" asp-action="Index">
                <img class="auth-panel-brand-mark" src="~/images/images.jpg" asp-append-version="true" alt="@Context.T("layout.logo_alt")" />
                <span class="auth-panel-brand-text">@Context.T("layout.brand_name")</span>
            </a>
            <div class="auth-panel-content">
                <h2>@Context.T("auth.login_panel_title")</h2>
                <p>@Context.T("auth.login_panel_desc")</p>
            </div>
            <a class="auth-signup-cta js-auth-switch" data-auth-switch-direction="right" asp-controller="Auth" asp-action="Login" asp-route-returnUrl="@Model.ReturnUrl">@Context.T("auth.login_panel_cta")</a>
        </aside>

        <div class="auth-split-left">
            <div class="auth-content-head">
                <p class="auth-kicker">@Context.T("auth.access")</p>
                <h1 class="auth-heading" id="auth-register-heading">@Context.T("auth.register_title")</h1>
                <p class="auth-copy">@Context.T("auth.register_subtitle")</p>
            </div>

            <form class="auth-form" method="post" asp-controller="Auth" asp-action="Register">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="ReturnUrl" />

                <div class="auth-form-grid auth-form-grid-2">
                    <div class="form-field">
                        <label class="auth-label" for="register-display-name">@Context.T("auth.display_name")</label>
                        <input class="auth-input"
                               id="register-display-name"
                               asp-for="DisplayName"
                               autocomplete="name"
                               required
                               placeholder="@Context.T("auth.placeholder.full_name")" />
                    </div>

                    <div class="form-field">
                        <label class="auth-label" for="register-username">@Context.T("auth.username")</label>
                        <input class="auth-input"
                               id="register-username"
                               asp-for="Username"
                               autocomplete="username"
                               autocapitalize="none"
                               autocorrect="off"
                               spellcheck="false"
                               inputmode="text"
                               enterkeyhint="next"
                               required
                               placeholder="@Context.T("auth.placeholder.new_username")" />
                    </div>
                </div>

                <div class="auth-form-grid auth-form-grid-2">
                    <div class="form-field">
                        <label class="auth-label" for="register-password">@Context.T("auth.password")</label>
                        <input class="auth-input"
                               id="register-password"
                               asp-for="Password"
                               type="password"
                               autocomplete="new-password"
                               enterkeyhint="next"
                               required
                               placeholder="********" />
                    </div>

                    <div class="form-field">
                        <label class="auth-label" for="register-confirm-password">@Context.T("auth.confirm_password")</label>
                        <input class="auth-input"
                               id="register-confirm-password"
                               asp-for="ConfirmPassword"
                               type="password"
                               autocomplete="new-password"
                               enterkeyhint="done"
                               required
                               placeholder="********" />
                    </div>
                </div>

                @if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
                {
                    <div class="alert alert-error" aria-live="polite">
                        @Model.ErrorMessage
                    </div>
                }

                <button class="auth-login-btn" type="submit">@Context.T("auth.submit_register")</button>
                <p class="auth-register-copy">
                    @Context.T("auth.have_account")
                    <a class="js-auth-switch" data-auth-switch-direction="right" asp-controller="Auth" asp-action="Login" asp-route-returnUrl="@Model.ReturnUrl">@Context.T("auth.login")</a>
                </p>
            </form>
        </div>
    </section>
</div>
