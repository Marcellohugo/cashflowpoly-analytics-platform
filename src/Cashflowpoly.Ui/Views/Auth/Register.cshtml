@model Cashflowpoly.Ui.Models.RegisterViewModel

@{
    ViewData["Title"] = Context.T("auth.register_title");
}

<section class="section-shell">
    <div class="mx-auto max-w-xl">
        <div>
            <p class="eyebrow text-emerald-700">@Context.T("auth.access")</p>
            <h1 class="headline">@Context.T("auth.register_title")</h1>
            <p class="subhead">@Context.T("auth.register_subtitle")</p>
        </div>

        <form class="mt-6 glass-surface p-6" method="post" asp-controller="Auth" asp-action="Register">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="ReturnUrl" />

            <div>
                <label class="text-xs font-semibold uppercase tracking-wider text-slate-500">@Context.T("auth.username")</label>
                <input class="mt-2 w-full rounded-xl border border-slate-200 bg-white/80 px-4 py-3 text-sm focus:border-slate-400 focus:outline-none"
                       asp-for="Username" placeholder="player_baru" />
            </div>

            <div class="mt-4">
                <label class="text-xs font-semibold uppercase tracking-wider text-slate-500">@Context.T("auth.password")</label>
                <input class="mt-2 w-full rounded-xl border border-slate-200 bg-white/80 px-4 py-3 text-sm focus:border-slate-400 focus:outline-none"
                       asp-for="Password" type="password" placeholder="********" />
            </div>

            <div class="mt-4">
                <label class="text-xs font-semibold uppercase tracking-wider text-slate-500">@Context.T("auth.confirm_password")</label>
                <input class="mt-2 w-full rounded-xl border border-slate-200 bg-white/80 px-4 py-3 text-sm focus:border-slate-400 focus:outline-none"
                       asp-for="ConfirmPassword" type="password" placeholder="********" />
            </div>

            <div class="mt-4">
                <label class="text-xs font-semibold uppercase tracking-wider text-slate-500">@Context.T("auth.role")</label>
                <select class="mt-2 w-full rounded-xl border border-slate-200 bg-white/80 px-4 py-3 text-sm focus:border-slate-400 focus:outline-none"
                        asp-for="Role">
                    <option value="PLAYER">@Context.T("auth.role_player")</option>
                    <option value="INSTRUCTOR">@Context.T("auth.role_instructor")</option>
                </select>
            </div>

            @if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
            {
                <div class="mt-4 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">
                    @Model.ErrorMessage
                </div>
            }

            <button class="btn-primary mt-6" type="submit">@Context.T("auth.submit_register")</button>
            <p class="mt-3 text-sm text-slate-500">
                @Context.T("auth.have_account")
                <a class="font-semibold hover:underline" asp-controller="Auth" asp-action="Login" asp-route-returnUrl="@Model.ReturnUrl">@Context.T("auth.login")</a>
            </p>
        </form>
    </div>
</section>
