@* Fungsi file: Merender tampilan Razor untuk bagian Rulesets/Create.cshtml beserta interaksi presentasinya.  Penjelasan rinci per baris. Nomor berikut mengacu kode asli setelah blok komentar ini (header Fungsi file dihitung sebagai baris 1). Komentar server-side tidak dikirim ke browser.
Baris sumber 2: Menetapkan model tampilan bertipe `Cashflowpoly.Ui.Models.CreateRulesetViewModel`; Razor memeriksa akses properti Model saat kompilasi dan controller memasok instance model saat rendering.
Baris sumber 4: Membuka blok C# Razor untuk menghitung data tampilan di server sebelum markup halaman dikirim kepada browser.
Baris sumber 5: Menyimpan `isEditMode` dengan membaca nilai `Model.IsEditMode && Model.RulesetId.HasValue` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya.
Baris sumber 6: Menyimpan `formActionUrl` dengan membaca nilai `isEditMode` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya.
Baris sumber 7: Menentukan hasil ketika kondisi ternary sebelumnya benar, yaitu `Url.Action(”Edit”, ”Rulesets”, new ｛ rulesetId ＝ Model.RulesetId ｝)`; nilai ini menjadi keluaran ekspresi pilihan.
Baris sumber 8: Menentukan hasil ketika kondisi ternary sebelumnya salah, yaitu `Url.Action(”Create”, ”Rulesets”);`; nilai cadangan ini melengkapi ekspresi pilihan.
Baris sumber 9: Memperbarui `ViewData[”Title”]` dengan mengambil teks terjemahan berkunci `rulesets.edit_title` sesuai bahasa sesi pengguna.
Baris sumber 10: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 12: Membentuk elemen `section` sebagai bagian halaman dengan topik konten tersendiri. Atribut `class` bernilai `section-shell` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 13: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `action-toolbar mb-4` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 14: Memeriksa kondisi `isEditMode`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 15: Membuka ruang lingkup blok untuk `div`; deklarasi lokal dan urutan langkah di dalamnya dikelompokkan di sini.
Baris sumber 16: Membentuk elemen `a` sebagai tautan navigasi ke halaman atau sasaran lain. Atribut `class` bernilai `btn-ghost` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Isi `‹a class＝”btn-ghost”` menjadi teks yang terlihat pada komponen.
Baris sumber 16: Baris sumber 17: Melengkapi atribut `asp-controller` pada `a` dengan `”Rulesets”`; nama controller tujuan yang dipakai tag helper pembuat URL.
Baris sumber 16: Baris sumber 18: Melengkapi atribut `asp-action` pada `a` dengan `”Details”`; nama action tujuan yang dipakai tag helper pembuat URL.
Baris sumber 16: Baris sumber 19: Melengkapi atribut `asp-route-rulesetId` pada `a` dengan `”@Model.RulesetId”›@(”‹- ” + Context.T(”rulesets.navigasi kembali ke_list”))‹/a›`; tag helper menggunakan nilai ini sebagai parameter route URL.
Baris sumber 20: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 21: Memulai cabang alternatif yang hanya diproses apabila kondisi if sebelumnya tidak terpenuhi.
Baris sumber 22: Membuka ruang lingkup blok untuk `a`; deklarasi lokal dan urutan langkah di dalamnya dikelompokkan di sini.
Baris sumber 23: Membentuk elemen `a` sebagai tautan navigasi ke halaman atau sasaran lain. Atribut `class` bernilai `btn-ghost` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `asp-controller` bernilai `Rulesets` menentukan nama controller tujuan yang dipakai tag helper pembuat URL. Atribut `asp-action` bernilai `Index` menentukan nama action tujuan yang dipakai tag helper pembuat URL. Teks dinamis dibaca dari kunci terjemahan `rulesets.navigasi kembali ke_list` menurut bahasa sesi.
Baris sumber 24: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 25: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 27: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `page-intro` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 28: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `page-intro-main` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 29: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `eyebrow` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.management` menurut bahasa sesi.
Baris sumber 30: Membentuk elemen `h1` sebagai judul utama halaman. Atribut `class` bernilai `headline` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.edit_title`, `rulesets.create_title` menurut bahasa sesi.
Baris sumber 31: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `subhead` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.edit_subtitle`, `rulesets.create_subtitle` menurut bahasa sesi.
Baris sumber 32: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 33: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 35: Memeriksa kondisi `!string.IsNullOrWhiteSpace(Model.ErrorMessage)`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 36: Membuka ruang lingkup blok untuk `p`; deklarasi lokal dan urutan langkah di dalamnya dikelompokkan di sini.
Baris sumber 37: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `mt-4 alert alert-error` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 38: Menyisipkan hasil ekspresi Razor `@Model.ErrorMessage` ke HTML; nilai teks standar di-encode oleh Razor kecuali helper secara eksplisit menghasilkan HTML.
Baris sumber 39: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 40: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 42: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `mt-6 ruleset-unified` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 43: Membentuk elemen `section` sebagai bagian halaman dengan topik konten tersendiri. Atribut `class` bernilai `ruleset-form-section` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 44: Membentuk elemen `details` sebagai kontainer yang dapat diperluas dan diciutkan browser. Atribut `class` bernilai `mt-3 data-toggle` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 45: Membentuk elemen `summary` sebagai pemicu buka/tutup untuk elemen details. Teks dinamis dibaca dari kunci terjemahan `rulesets.tips.form_title` menurut bahasa sesi.
Baris sumber 46: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `mt-3 metric-panel` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 47: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `metric-list` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 48: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-value` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.step`, `rulesets.tip.versioning_draft` menurut bahasa sesi.
Baris sumber 49: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-value` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.step`, `rulesets.tip.consistency` menurut bahasa sesi.
Baris sumber 50: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-value` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `sessions.step`, `rulesets.tip.safety_before_start` menurut bahasa sesi.
Baris sumber 51: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 52: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 53: Menutup elemen `details` (kontainer yang dapat diperluas dan diciutkan browser) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 54: Menutup elemen `section` (bagian halaman dengan topik konten tersendiri) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 56: Membentuk elemen `form` sebagai kelompok kontrol masukan yang dikirim ke endpoint server. Atribut `class` bernilai `mt-4 form-grid ruleset-create-form` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `method` bernilai `post` menentukan metode HTTP ketika formulir dikirim. Ketika dikirim, nama dan nilai kontrol di dalamnya digunakan model binding pada server.
Baris sumber 57: Menghasilkan bagian tampilan melalui `@Html.AntiForgeryToken()`; Razor menjalankan helper di server dan menyisipkan hasil pada posisi markup ini.
Baris sumber 58: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `form-field` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 59: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `form-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `common.name` menurut bahasa sesi.
Baris sumber 60: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `class` bernilai `form-input` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Isi `‹input class＝”form-input”` menjadi teks yang terlihat pada komponen.
Baris sumber 60: Baris sumber 61: Melengkapi atribut `id` pada `input` dengan `”ruleset-name”`; identitas unik elemen untuk selector, label, atau sasaran tautan.
Baris sumber 60: Baris sumber 62: Melengkapi atribut `asp-for` pada `input` dengan `”Name”`; properti model yang diikat ke kontrol formulir.
Baris sumber 63: Melengkapi atribut `required` pada `input` dengan `/›`; kewajiban pengisian kontrol pada validasi browser.
Baris sumber 64: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 65: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `form-field` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 66: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `form-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `common.description` menurut bahasa sesi.
Baris sumber 67: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `class` bernilai `form-input` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Isi `‹input class＝”form-input”` menjadi teks yang terlihat pada komponen.
Baris sumber 67: Baris sumber 68: Melengkapi atribut `id` pada `input` dengan `”ruleset-description”`; identitas unik elemen untuk selector, label, atau sasaran tautan.
Baris sumber 67: Baris sumber 69: Melengkapi atribut `asp-for` pada `input` dengan `”Description”`; properti model yang diikat ke kontrol formulir.
Baris sumber 67: Baris sumber 70: Melengkapi atribut `placeholder` pada `input` dengan `”@Context.T(”rulesets.form.description_placeholder”)” /›`; contoh atau petunjuk masukan sebelum pengguna mengetik.
Baris sumber 71: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `mt-1 text-xs text-slate-500` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.description_hint` menurut bahasa sesi.
Baris sumber 72: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 74: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `ruleset-form-section` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 75: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `ruleset-section-head` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 76: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `ruleset-section-title` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.core_setup` menurut bahasa sesi.
Baris sumber 77: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `ruleset-section-subtitle` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.help.core_setup` menurut bahasa sesi.
Baris sumber 78: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 79: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `grid gap-3 md:grid-cols-2 mt-3` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 80: Membentuk elemen `fieldset` sebagai pengelompokan kontrol formulir terkait. Atribut `class` bernilai `md:col-span-2` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 81: Membentuk elemen `legend` sebagai judul kelompok kontrol formulir. Atribut `class` bernilai `sr-only` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.mode` menurut bahasa sesi.
Baris sumber 82: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `ruleset-section-head` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 83: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `ruleset-section-title` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.mode` menurut bahasa sesi.
Baris sumber 84: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `ruleset-section-subtitle ruleset-mode-picker-hint` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.mode_choice_hint` menurut bahasa sesi.
Baris sumber 85: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 86: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `ruleset-mode-picker mt-2` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 87: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `ruleset-mode-option ruleset-mode-option--beginner` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 88: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `class` bernilai `ruleset-mode-radio` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `type` bernilai `radio` menentukan jenis kontrol atau tipe konten. Atribut `name` bernilai `cfg-mode` menentukan nama field yang dikirim atau nama metadata.
Baris sumber 89: Membentuk elemen `span` sebagai wadah teks inline tanpa memulai blok baru. Atribut `class` bernilai `ruleset-mode-option-content` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 90: Membentuk elemen `span` sebagai wadah teks inline tanpa memulai blok baru. Atribut `class` bernilai `ruleset-mode-badge ruleset-mode-badge--beginner` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.mode_beginner` menurut bahasa sesi.
Baris sumber 91: Membentuk elemen `span` sebagai wadah teks inline tanpa memulai blok baru. Atribut `class` bernilai `ruleset-mode-option-copy` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.quick_guide_beginner` menurut bahasa sesi.
Baris sumber 92: Menutup elemen `span` (wadah teks inline tanpa memulai blok baru) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 93: Menutup elemen `label` (label untuk menjelaskan kontrol formulir) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 94: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `ruleset-mode-option ruleset-mode-option--advanced` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 95: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `class` bernilai `ruleset-mode-radio` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `type` bernilai `radio` menentukan jenis kontrol atau tipe konten. Atribut `name` bernilai `cfg-mode` menentukan nama field yang dikirim atau nama metadata.
Baris sumber 96: Membentuk elemen `span` sebagai wadah teks inline tanpa memulai blok baru. Atribut `class` bernilai `ruleset-mode-option-content` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 97: Membentuk elemen `span` sebagai wadah teks inline tanpa memulai blok baru. Atribut `class` bernilai `ruleset-mode-badge ruleset-mode-badge--advanced` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.mode_advanced` menurut bahasa sesi.
Baris sumber 98: Membentuk elemen `span` sebagai wadah teks inline tanpa memulai blok baru. Atribut `class` bernilai `ruleset-mode-option-copy` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.quick_guide_advanced` menurut bahasa sesi.
Baris sumber 99: Menutup elemen `span` (wadah teks inline tanpa memulai blok baru) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 100: Menutup elemen `label` (label untuk menjelaskan kontrol formulir) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 101: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 102: Menutup elemen `fieldset` (pengelompokan kontrol formulir terkait) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 103: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen.
Baris sumber 104: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `form-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.actions_per_turn` menurut bahasa sesi.
Baris sumber 105: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `id` bernilai `cfg-actions` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `number` menentukan jenis kontrol atau tipe konten. Atribut `class` bernilai `form-input` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 106: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 107: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen.
Baris sumber 108: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `form-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.starting_cash` menurut bahasa sesi.
Baris sumber 109: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `id` bernilai `cfg-cash` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `number` menentukan jenis kontrol atau tipe konten. Atribut `class` bernilai `form-input` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 110: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 111: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `md:col-span-2` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 112: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `form-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.player_ordering` menurut bahasa sesi.
Baris sumber 113: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `id` bernilai `cfg-player-ordering-display` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Isi `‹input id＝”cfg-player-ordering-display”` menjadi teks yang terlihat pada komponen.
Baris sumber 113: Baris sumber 114: Melengkapi atribut `type` pada `input` dengan `”text”`; jenis kontrol atau tipe konten.
Baris sumber 113: Baris sumber 116: Melengkapi atribut `class` pada `input` dengan `”form-input”`; kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 113: Baris sumber 117: Melengkapi atribut `value` pada `input` dengan `”@Context.T(”rulesets.form.player_ordering_player_order”)” /›`; nilai awal atau nilai yang dikirim oleh kontrol.
Baris sumber 115: Menyediakan teks atau fragmen konten `readonly` pada bagian tampilan ini; posisinya mengikuti struktur elemen dan kondisi Razor yang membungkusnya.
Baris sumber 118: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `id` bernilai `cfg-player-ordering` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `hidden` menentukan jenis kontrol atau tipe konten.
Baris sumber 119: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `mt-1 text-xs text-slate-500` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.player_ordering_fixed_hint` menurut bahasa sesi.
Baris sumber 120: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 121: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen.
Baris sumber 122: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `form-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.freelance_income` menurut bahasa sesi.
Baris sumber 123: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `id` bernilai `cfg-freelance-income` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `number` menentukan jenis kontrol atau tipe konten. Atribut `class` bernilai `form-input` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 124: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 125: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 126: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 128: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `ruleset-form-section` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 129: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `ruleset-section-head` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 130: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `ruleset-section-title` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.weekday_features` menurut bahasa sesi.
Baris sumber 131: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `ruleset-section-subtitle` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.help.weekday_features` menurut bahasa sesi.
Baris sumber 132: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 133: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `grid gap-3 md:grid-cols-3 mt-3` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 134: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `id` bernilai `cfg-friday` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `checkbox` menentukan jenis kontrol atau tipe konten. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.friday_donation` menurut bahasa sesi.
Baris sumber 135: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `id` bernilai `cfg-saturday` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `checkbox` menentukan jenis kontrol atau tipe konten. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.saturday_gold_trade` menurut bahasa sesi.
Baris sumber 136: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `id` bernilai `cfg-sunday` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `checkbox` menentukan jenis kontrol atau tipe konten. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.sunday_rest` menurut bahasa sesi.
Baris sumber 137: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 138: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 140: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `ruleset-form-section` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 141: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `ruleset-section-head` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 142: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `ruleset-section-title` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.constraints` menurut bahasa sesi.
Baris sumber 143: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `ruleset-section-subtitle` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.help.constraints` menurut bahasa sesi.
Baris sumber 144: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 145: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `grid gap-3 md:grid-cols-2 mt-3` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 146: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen.
Baris sumber 147: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `form-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.cash_min` menurut bahasa sesi.
Baris sumber 148: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `id` bernilai `cfg-cash-min` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `number` menentukan jenis kontrol atau tipe konten. Atribut `class` bernilai `form-input` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 149: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 150: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen.
Baris sumber 151: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `form-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.max_ingredient_total` menurut bahasa sesi.
Baris sumber 152: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `id` bernilai `cfg-max-ing-total` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `number` menentukan jenis kontrol atau tipe konten. Atribut `class` bernilai `form-input` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 153: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 154: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen.
Baris sumber 155: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `form-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.max_same_ingredient` menurut bahasa sesi.
Baris sumber 156: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `id` bernilai `cfg-max-same-ing` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `number` menentukan jenis kontrol atau tipe konten. Atribut `class` bernilai `form-input` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 157: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 158: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 159: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 161: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `ruleset-form-section` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 162: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `ruleset-section-head` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 163: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `ruleset-section-title` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.economy_and_donation` menurut bahasa sesi.
Baris sumber 164: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Atribut `class` bernilai `ruleset-section-subtitle` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.help.economy_and_donation` menurut bahasa sesi.
Baris sumber 165: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 166: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `grid gap-3 md:grid-cols-2 mt-3` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 167: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen.
Baris sumber 168: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `form-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.donation_min` menurut bahasa sesi.
Baris sumber 169: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `id` bernilai `cfg-donation-min` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `number` menentukan jenis kontrol atau tipe konten. Atribut `class` bernilai `form-input` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 170: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 171: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen.
Baris sumber 172: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `form-label` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.donation_max` menurut bahasa sesi.
Baris sumber 173: Menyediakan elemen `input` sebagai kontrol masukan satu nilai. Atribut `id` bernilai `cfg-donation-max` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `number` menentukan jenis kontrol atau tipe konten. Atribut `class` bernilai `form-input` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 174: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 175: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `id` bernilai `cfg-gold-buy` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `checkbox` menentukan jenis kontrol atau tipe konten. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.gold_buy` menurut bahasa sesi.
Baris sumber 176: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `id` bernilai `cfg-gold-sell` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `checkbox` menentukan jenis kontrol atau tipe konten. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.gold_sell` menurut bahasa sesi.
Baris sumber 177: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `ruleset-advanced-feature-grid md:col-span-2` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 178: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `ruleset-advanced-feature-head` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 179: Membentuk elemen `span` sebagai wadah teks inline tanpa memulai blok baru. Atribut `class` bernilai `ruleset-mode-badge ruleset-mode-badge--advanced` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.mode_advanced` menurut bahasa sesi.
Baris sumber 180: Membentuk elemen `p` sebagai paragraf atau label penjelasan. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.advanced_features_hint` menurut bahasa sesi.
Baris sumber 181: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 182: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `grid gap-3 md:grid-cols-3 mt-3` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 183: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `id` bernilai `cfg-adv-loan` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `checkbox` menentukan jenis kontrol atau tipe konten. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.loan_enabled` menurut bahasa sesi.
Baris sumber 184: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `id` bernilai `cfg-adv-insurance` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `checkbox` menentukan jenis kontrol atau tipe konten. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.insurance_enabled` menurut bahasa sesi.
Baris sumber 185: Membentuk elemen `label` sebagai label untuk menjelaskan kontrol formulir. Atribut `class` bernilai `metric-row` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `class` bernilai `metric-key` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `id` bernilai `cfg-adv-saving-goal` menentukan identitas unik elemen untuk selector, label, atau sasaran tautan. Atribut `type` bernilai `checkbox` menentukan jenis kontrol atau tipe konten. Teks dinamis dibaca dari kunci terjemahan `rulesets.form.saving_goal_enabled` menurut bahasa sesi.
Baris sumber 186: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 187: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 188: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 189: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 191: Membentuk elemen `textarea` sebagai kontrol masukan teks banyak baris. Atribut `asp-for` bernilai `DefinitionJson` menentukan properti model yang diikat ke kontrol formulir. Atribut `class` bernilai `hidden` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 192: Membentuk elemen `div` sebagai kontainer pengelompokan layout dan komponen. Atribut `class` bernilai `form-actions` menentukan kelas CSS untuk memilih gaya dan hook interaksi.
Baris sumber 193: Membentuk elemen `button` sebagai kontrol interaksi yang dapat diaktifkan pengguna. Atribut `class` bernilai `btn-primary` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Isi `‹button class＝”btn-primary”` menjadi teks yang terlihat pada komponen.
Baris sumber 193: Baris sumber 194: Melengkapi atribut `type` pada `button` dengan `”submit”›@(isEditMode ? Context.T(”rulesets.update”) : Context.T(”rulesets.save”))‹/button›`; jenis kontrol atau tipe konten.
Baris sumber 195: Memeriksa kondisi `isEditMode`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 196: Membuka ruang lingkup blok untuk `button`; deklarasi lokal dan urutan langkah di dalamnya dikelompokkan di sini.
Baris sumber 197: Membentuk elemen `a` sebagai tautan navigasi ke halaman atau sasaran lain. Atribut `class` bernilai `text-link` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Isi `‹a class＝”text-link”` menjadi teks yang terlihat pada komponen.
Baris sumber 197: Baris sumber 198: Melengkapi atribut `asp-controller` pada `a` dengan `”Rulesets”`; nama controller tujuan yang dipakai tag helper pembuat URL.
Baris sumber 197: Baris sumber 199: Melengkapi atribut `asp-action` pada `a` dengan `”Details”`; nama action tujuan yang dipakai tag helper pembuat URL.
Baris sumber 197: Baris sumber 200: Melengkapi atribut `asp-route-rulesetId` pada `a` dengan `”@Model.RulesetId”›@Context.T(”common.cancel”)‹/a›`; tag helper menggunakan nilai ini sebagai parameter route URL.
Baris sumber 201: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 202: Memulai cabang alternatif yang hanya diproses apabila kondisi if sebelumnya tidak terpenuhi.
Baris sumber 203: Membuka ruang lingkup blok untuk `a`; deklarasi lokal dan urutan langkah di dalamnya dikelompokkan di sini.
Baris sumber 204: Membentuk elemen `a` sebagai tautan navigasi ke halaman atau sasaran lain. Atribut `class` bernilai `text-link` menentukan kelas CSS untuk memilih gaya dan hook interaksi. Atribut `asp-controller` bernilai `Rulesets` menentukan nama controller tujuan yang dipakai tag helper pembuat URL. Atribut `asp-action` bernilai `Index` menentukan nama action tujuan yang dipakai tag helper pembuat URL. Teks dinamis dibaca dari kunci terjemahan `common.cancel` menurut bahasa sesi.
Baris sumber 205: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 206: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 207: Menutup elemen `form` (kelompok kontrol masukan yang dikirim ke endpoint server) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 208: Menutup elemen `div` (kontainer pengelompokan layout dan komponen) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 209: Menutup elemen `section` (bagian halaman dengan topik konten tersendiri) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 211: Mendefinisikan section Razor `Scripts ｛`; layout merender bagian ini pada lokasi RenderSectionAsync yang sesuai.
Baris sumber 212: Membentuk elemen `script` sebagai skrip browser atau referensi berkas JavaScript.
Baris sumber 213: Mendefinisikan fungsi atau pemetaan `(() → ｛`; parameter di sisi kiri → dipakai badan di sisi kanan ketika fungsi dipanggil atau pola cocok.
Baris sumber 214: Menyimpan `getEl` dengan mengambil elemen DOM ber-ID `id` agar data atau event dapat dihubungkan ke komponen yang tepat. Nilai berupa fungsi; badan callback dijalankan hanya ketika fungsi dipanggil.
Baris sumber 215: Menyimpan `toInt` dengan membaca nilai `(id, fallback ＝ 0) → ｛` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya. Nilai berupa fungsi; badan callback dijalankan hanya ketika fungsi dipanggil.
Baris sumber 216: Menyimpan `v` dengan mengevaluasi `parseInt(getEl(id)?.value ?? ””, 10)`; operator ?? memakai nilai cadangan hanya ketika sisi kiri null atau undefined.
Baris sumber 217: Mengembalikan hasil kepada pemanggil dengan memastikan `v` merupakan angka berhingga sehingga NaN dan Infinity tidak masuk perhitungan atau grafik; eksekusi fungsi berakhir setelah nilai dihitung.
Baris sumber 218: Menutup blok atau objek yang sedang disusun; tanda titik koma mengakhiri pernyataan.
Baris sumber 219: Menyimpan `toBool` dengan membaca `(id) → !!getEl(id)?.checked` dengan akses opsional sehingga properti pada objek kosong tidak menyebabkan kegagalan. Nilai berupa fungsi; badan callback dijalankan hanya ketika fungsi dipanggil.
Baris sumber 220: Menyimpan `configTextArea` dengan memanggil `getEl(”DefinitionJson”)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 221: Menyimpan `formEl` dengan mencari elemen DOM pertama yang sesuai dengan `”[data-ruleset-create-form]”`; hasil dapat null bila komponen tidak ada pada halaman.
Baris sumber 222: Menyimpan `modeInputs` dengan mencari seluruh elemen DOM yang cocok dengan selector `'input[name＝”cfg-mode”]')`; daftar ini menjadi sasaran perilaku antarmuka.
Baris sumber 223: Menyimpan `getMode` dengan mencari elemen pertama yang memenuhi `(input) → input.checked)?.value ?? ”PEMULA”`; hasil kosong ditangani oleh kode pemanggil. Nilai berupa fungsi; badan callback dijalankan hanya ketika fungsi dipanggil.
Baris sumber 224: Menyimpan `advancedFeaturePanel` dengan mencari elemen DOM pertama yang sesuai dengan `”[data-ruleset-advanced-features]”`; hasil dapat null bila komponen tidak ada pada halaman.
Baris sumber 225: Menyimpan `advancedFeatureInputs` dengan memanggil `[getEl(”cfg-adv-loan”), getEl(”cfg-adv-insurance”), getEl(”cfg-adv-saving-goal”)]` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 226: Menyimpan `modePresets` dengan menyiapkan objek sebagai wadah pasangan properti dan nilai yang diisi pada baris berikutnya.
Baris sumber 227: Mengisi properti `PEMULA` pada objek atau konfigurasi dengan menyiapkan objek sebagai wadah pasangan properti dan nilai yang diisi pada baris berikutnya; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 228: Mengisi properti `starting_cash` pada objek atau konfigurasi dengan menggunakan konstanta numerik `20` sebagai nilai awal atau parameter perhitungan; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 229: Mengisi properti `loan_enabled` pada objek atau konfigurasi dengan menonaktifkan flag Boolean dengan nilai false; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 230: Mengisi properti `insurance_enabled` pada objek atau konfigurasi dengan menonaktifkan flag Boolean dengan nilai false; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 231: Mengisi properti `saving_goal_enabled` pada objek atau konfigurasi dengan menonaktifkan flag Boolean dengan nilai false; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 232: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 233: Mengisi properti `MAHIR` pada objek atau konfigurasi dengan menyiapkan objek sebagai wadah pasangan properti dan nilai yang diisi pada baris berikutnya; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 234: Mengisi properti `starting_cash` pada objek atau konfigurasi dengan menggunakan konstanta numerik `10` sebagai nilai awal atau parameter perhitungan; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 235: Mengisi properti `loan_enabled` pada objek atau konfigurasi dengan mengaktifkan flag Boolean dengan nilai true; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 236: Mengisi properti `insurance_enabled` pada objek atau konfigurasi dengan mengaktifkan flag Boolean dengan nilai true; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 237: Mengisi properti `saving_goal_enabled` pada objek atau konfigurasi dengan mengaktifkan flag Boolean dengan nilai true; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 238: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 239: Menutup blok atau objek yang sedang disusun; tanda titik koma mengakhiri pernyataan.
Baris sumber 241: Menyimpan `scoreDefaults` dengan menyiapkan objek sebagai wadah pasangan properti dan nilai yang diisi pada baris berikutnya.
Baris sumber 242: Mengisi properti `donation_rank_points` pada objek atau konfigurasi dengan membaca nilai `[｛ rank: 1, points: 7 ｝, ｛ rank: 2, points: 5 ｝, ｛ rank: 3, points: 2 ｝]` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 243: Mengisi properti `gold_points_by_qty` pada objek atau konfigurasi dengan membaca nilai `[｛ qty: 1, points: 3 ｝, ｛ qty: 2, points: 5 ｝, ｛ qty: 3, points: 8 ｝, ｛ qty: 4, points: 12 ｝]` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 244: Mengisi properti `pension_rank_points` pada objek atau konfigurasi dengan membaca nilai `[｛ rank: 1, points: 5 ｝, ｛ rank: 2, points: 3 ｝, ｛ rank: 3, points: 1 ｝]` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 245: Menutup blok atau objek yang sedang disusun; tanda titik koma mengakhiri pernyataan.
Baris sumber 247: Menyimpan `readForm` dengan membaca nilai `() → (｛` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya. Nilai berupa fungsi; badan callback dijalankan hanya ketika fungsi dipanggil.
Baris sumber 248: Mengisi properti `mode` pada objek atau konfigurasi dengan memanggil `getMode()` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 249: Mengisi properti `actions_per_turn` pada objek atau konfigurasi dengan memanggil `toInt(”cfg-actions”, 2)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 250: Mengisi properti `starting_cash` pada objek atau konfigurasi dengan memanggil `toInt(”cfg-cash”, 20)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 251: Mengisi properti `player_ordering` pada objek atau konfigurasi dengan menggunakan literal `”PLAYER_ORDER”` sebagai teks, kunci, warna, atau isi template sesuai tempat pemakaiannya; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 252: Mengisi properti `weekday_rules` pada objek atau konfigurasi dengan menyiapkan objek sebagai wadah pasangan properti dan nilai yang diisi pada baris berikutnya; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 253: Mengisi properti `friday` pada objek atau konfigurasi dengan memanggil `｛ feature: ”DONATION”, enabled: toBool(”cfg-friday”) ｝` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 254: Mengisi properti `saturday` pada objek atau konfigurasi dengan memanggil `｛ feature: ”GOLD_TRADE”, enabled: toBool(”cfg-saturday”) ｝` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 255: Mengisi properti `sunday` pada objek atau konfigurasi dengan memanggil `｛ feature: ”REST”, enabled: toBool(”cfg-sunday”) ｝` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 256: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 257: Mengisi properti `constraints` pada objek atau konfigurasi dengan menyiapkan objek sebagai wadah pasangan properti dan nilai yang diisi pada baris berikutnya; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 258: Mengisi properti `cash_min` pada objek atau konfigurasi dengan memanggil `toInt(”cfg-cash-min”, 0)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 259: Mengisi properti `max_ingredient_total` pada objek atau konfigurasi dengan memanggil `toInt(”cfg-max-ing-total”, 6)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 260: Mengisi properti `max_same_ingredient` pada objek atau konfigurasi dengan memanggil `toInt(”cfg-max-same-ing”, 3)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 261: Mengisi properti `primary_need_max_per_day` pada objek atau konfigurasi dengan menggunakan null sebagai penanda bahwa objek atau pilihan belum tersedia; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 262: Mengisi properti `require_primary_before_others` pada objek atau konfigurasi dengan mengaktifkan flag Boolean dengan nilai true; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 263: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 264: Mengisi properti `donation` pada objek atau konfigurasi dengan menyiapkan objek sebagai wadah pasangan properti dan nilai yang diisi pada baris berikutnya; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 265: Mengisi properti `min_amount` pada objek atau konfigurasi dengan memanggil `toInt(”cfg-donation-min”, 1)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 266: Mengisi properti `max_amount` pada objek atau konfigurasi dengan memanggil `toInt(”cfg-donation-max”, 999999)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 267: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 268: Mengisi properti `gold_trade` pada objek atau konfigurasi dengan menyiapkan objek sebagai wadah pasangan properti dan nilai yang diisi pada baris berikutnya; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 269: Mengisi properti `allow_buy` pada objek atau konfigurasi dengan memanggil `toBool(”cfg-gold-buy”)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 270: Mengisi properti `allow_sell` pada objek atau konfigurasi dengan memanggil `toBool(”cfg-gold-sell”)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 271: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 272: Mengisi properti `advanced` pada objek atau konfigurasi dengan menyiapkan objek sebagai wadah pasangan properti dan nilai yang diisi pada baris berikutnya; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 273: Mengisi properti `loan` pada objek atau konfigurasi dengan memanggil `｛ enabled: getMode() ＝＝＝ ”MAHIR” && toBool(”cfg-adv-loan”) ｝` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 274: Mengisi properti `insurance` pada objek atau konfigurasi dengan memanggil `｛ enabled: getMode() ＝＝＝ ”MAHIR” && toBool(”cfg-adv-insurance”) ｝` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 275: Mengisi properti `saving_goal` pada objek atau konfigurasi dengan memanggil `｛ enabled: getMode() ＝＝＝ ”MAHIR” && toBool(”cfg-adv-saving-goal”) ｝` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 276: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 277: Mengisi properti `freelance` pada objek atau konfigurasi dengan memanggil `｛ income: toInt(”cfg-freelance-income”, 1) ｝` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 278: Mengisi properti `scoring` pada objek atau konfigurasi dengan membaca nilai `scoreDefaults` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya; konsumen objek membaca nilai ini melalui nama properti tersebut.
Baris sumber 279: Menutup blok atau objek yang sedang disusun; tanda titik koma mengakhiri pernyataan.
Baris sumber 281: Menyimpan `writeFormFromConfig` dengan membaca nilai `(cfg) → ｛` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya. Nilai berupa fungsi; badan callback dijalankan hanya ketika fungsi dipanggil.
Baris sumber 282: Memeriksa kondisi `!cfg || typeof cfg !＝＝ ”object”) return;`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 283: Menyimpan `resolvedMode` dengan menormalkan string menjadi huruf besar untuk menyamakan representasi kode atau kategori.
Baris sumber 284: Mendefinisikan fungsi atau pemetaan `modeInputs.forEach((input) → ｛`; parameter di sisi kiri → dipakai badan di sisi kanan ketika fungsi dipanggil atau pola cocok.
Baris sumber 285: Memperbarui `input.checked` dengan membaca nilai `input.value ＝＝＝ resolvedMode` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya.
Baris sumber 286: Menutup blok atau objek yang sedang disusun; tanda titik koma mengakhiri pernyataan.
Baris sumber 287: Melakukan operasi dengan mengevaluasi `getEl(”cfg-actions”).value ＝ cfg.actions_per_turn ?? 2`; operator ?? memakai nilai cadangan hanya ketika sisi kiri null atau undefined.
Baris sumber 288: Melakukan operasi dengan mengevaluasi `getEl(”cfg-cash”).value ＝ cfg.starting_cash ?? 20`; operator ?? memakai nilai cadangan hanya ketika sisi kiri null atau undefined.
Baris sumber 289: Melakukan operasi dengan mengevaluasi `getEl(”cfg-freelance-income”).value ＝ cfg.freelance?.income ?? 1`; operator ?? memakai nilai cadangan hanya ketika sisi kiri null atau undefined.
Baris sumber 290: Melakukan operasi dengan membaca `getEl(”cfg-friday”).checked ＝ !!cfg.weekday_rules?.friday?.enabled` dengan akses opsional sehingga properti pada objek kosong tidak menyebabkan kegagalan.
Baris sumber 291: Melakukan operasi dengan membaca `getEl(”cfg-saturday”).checked ＝ !!cfg.weekday_rules?.saturday?.enabled` dengan akses opsional sehingga properti pada objek kosong tidak menyebabkan kegagalan.
Baris sumber 292: Melakukan operasi dengan membaca `getEl(”cfg-sunday”).checked ＝ !!cfg.weekday_rules?.sunday?.enabled` dengan akses opsional sehingga properti pada objek kosong tidak menyebabkan kegagalan.
Baris sumber 293: Melakukan operasi dengan mengevaluasi `getEl(”cfg-cash-min”).value ＝ cfg.constraints?.cash_min ?? 0`; operator ?? memakai nilai cadangan hanya ketika sisi kiri null atau undefined.
Baris sumber 294: Melakukan operasi dengan mengevaluasi `getEl(”cfg-max-ing-total”).value ＝ cfg.constraints?.max_ingredient_total ?? 6`; operator ?? memakai nilai cadangan hanya ketika sisi kiri null atau undefined.
Baris sumber 295: Melakukan operasi dengan mengevaluasi `getEl(”cfg-max-same-ing”).value ＝ cfg.constraints?.max_same_ingredient ?? 3`; operator ?? memakai nilai cadangan hanya ketika sisi kiri null atau undefined.
Baris sumber 296: Melakukan operasi dengan mengevaluasi `getEl(”cfg-donation-min”).value ＝ cfg.donation?.min_amount ?? 1`; operator ?? memakai nilai cadangan hanya ketika sisi kiri null atau undefined.
Baris sumber 297: Melakukan operasi dengan mengevaluasi `getEl(”cfg-donation-max”).value ＝ cfg.donation?.max_amount ?? 999999`; operator ?? memakai nilai cadangan hanya ketika sisi kiri null atau undefined.
Baris sumber 298: Melakukan operasi dengan mengevaluasi `getEl(”cfg-gold-buy”).checked ＝ cfg.gold_trade?.allow_buy ?? true`; operator ?? memakai nilai cadangan hanya ketika sisi kiri null atau undefined.
Baris sumber 299: Melakukan operasi dengan mengevaluasi `getEl(”cfg-gold-sell”).checked ＝ cfg.gold_trade?.allow_sell ?? true`; operator ?? memakai nilai cadangan hanya ketika sisi kiri null atau undefined.
Baris sumber 300: Melakukan operasi dengan membaca `getEl(”cfg-adv-loan”).checked ＝ !!cfg.advanced?.loan?.enabled` dengan akses opsional sehingga properti pada objek kosong tidak menyebabkan kegagalan.
Baris sumber 301: Melakukan operasi dengan membaca `getEl(”cfg-adv-insurance”).checked ＝ !!cfg.advanced?.insurance?.enabled` dengan akses opsional sehingga properti pada objek kosong tidak menyebabkan kegagalan.
Baris sumber 302: Melakukan operasi dengan membaca `getEl(”cfg-adv-saving-goal”).checked ＝ !!cfg.advanced?.saving_goal?.enabled` dengan akses opsional sehingga properti pada objek kosong tidak menyebabkan kegagalan.
Baris sumber 303: Melakukan operasi dengan memanggil `setAdvancedFeatureAvailability(resolvedMode)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 304: Menutup blok atau objek yang sedang disusun; tanda titik koma mengakhiri pernyataan.
Baris sumber 306: Menyimpan `setAdvancedFeatureAvailability` dengan membaca nilai `(mode) → ｛` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya. Nilai berupa fungsi; badan callback dijalankan hanya ketika fungsi dipanggil.
Baris sumber 307: Menyimpan `isAdvanced` dengan menormalkan string menjadi huruf besar untuk menyamakan representasi kode atau kategori.
Baris sumber 308: Memeriksa kondisi `advancedFeaturePanel`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 309: Memperbarui `advancedFeaturePanel.hidden` dengan membaca nilai `!isAdvanced` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya.
Baris sumber 310: Melakukan operasi dengan menetapkan atribut elemen sesuai pasangan nama dan nilai `”aria-hidden”, `$｛!isAdvanced｝``; atribut mengendalikan presentasi atau aksesibilitas komponen.
Baris sumber 311: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 312: Mendefinisikan fungsi atau pemetaan `advancedFeatureInputs.forEach((input) → ｛`; parameter di sisi kiri → dipakai badan di sisi kanan ketika fungsi dipanggil atau pola cocok.
Baris sumber 313: Memeriksa kondisi `!input) return;`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 314: Memperbarui `input.disabled` dengan membaca nilai `!isAdvanced` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya.
Baris sumber 315: Memeriksa kondisi `!isAdvanced) input.checked ＝ false;`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 316: Menutup blok atau objek yang sedang disusun; tanda titik koma mengakhiri pernyataan.
Baris sumber 317: Menutup blok atau objek yang sedang disusun; tanda titik koma mengakhiri pernyataan.
Baris sumber 319: Menyimpan `applyModePreset` dengan membaca nilai `(mode) → ｛` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya. Nilai berupa fungsi; badan callback dijalankan hanya ketika fungsi dipanggil.
Baris sumber 320: Menyimpan `preset` dengan menormalkan string menjadi huruf besar untuk menyamakan representasi kode atau kategori.
Baris sumber 321: Memeriksa kondisi `!preset) return;`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 322: Melakukan operasi dengan memanggil `getEl(”cfg-cash”).value ＝ preset.starting_cash` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 323: Melakukan operasi dengan memanggil `getEl(”cfg-adv-loan”).checked ＝ preset.loan_enabled` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 324: Melakukan operasi dengan memanggil `getEl(”cfg-adv-insurance”).checked ＝ preset.insurance_enabled` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 325: Melakukan operasi dengan memanggil `getEl(”cfg-adv-saving-goal”).checked ＝ preset.saving_goal_enabled` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 326: Melakukan operasi dengan memanggil `setAdvancedFeatureAvailability(mode)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 327: Menutup blok atau objek yang sedang disusun; tanda titik koma mengakhiri pernyataan.
Baris sumber 329: Menyimpan `refreshConfig` dengan membaca nilai `() → ｛` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya. Nilai berupa fungsi; badan callback dijalankan hanya ketika fungsi dipanggil.
Baris sumber 330: Menyimpan `cfg` dengan memanggil `readForm()` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 331: Memeriksa kondisi `formEl) formEl.dataset.rulesetMode ＝ cfg.mode.toLowerCase();`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 332: Menyimpan `json` dengan mengubah `cfg, null, 2` menjadi teks JSON yang dapat disimpan atau dikirim.
Baris sumber 333: Memperbarui `configTextArea.value` dengan membaca nilai `json` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya.
Baris sumber 334: Menutup blok atau objek yang sedang disusun; tanda titik koma mengakhiri pernyataan.
Baris sumber 336: Memulai bagian yang dapat gagal, misalnya parsing atau permintaan data; kesalahan diteruskan ke catch yang menyertainya.
Baris sumber 337: Memeriksa kondisi `configTextArea?.value?.trim()`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 338: Melakukan operasi dengan mendeserialisasi string JSON `configTextArea.value)` menjadi objek JavaScript; JSON tidak valid akan masuk penanganan kesalahan.
Baris sumber 339: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
Baris sumber 340: Menangani kesalahan melalui catch `(_) ｛｝`; alur ini menyediakan pemulihan atau pesan kesalahan ketika operasi pada try gagal.
Baris sumber 342: Melakukan operasi dengan memanggil `setAdvancedFeatureAvailability(getMode())` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 344: Mendefinisikan fungsi atau pemetaan `document.querySelectorAll(”input, select”).forEach((el) → ｛`; parameter di sisi kiri → dipakai badan di sisi kanan ketika fungsi dipanggil atau pola cocok.
Baris sumber 345: Melakukan operasi dengan mendaftarkan penangan event `”input”, refreshConfig)`; fungsi callback akan dijalankan browser ketika event tersebut terjadi.
Baris sumber 346: Melakukan operasi dengan mendaftarkan penangan event `”change”, refreshConfig)`; fungsi callback akan dijalankan browser ketika event tersebut terjadi.
Baris sumber 347: Menutup blok atau objek yang sedang disusun; tanda titik koma mengakhiri pernyataan.
Baris sumber 349: Mendefinisikan fungsi atau pemetaan `modeInputs.forEach((input) → ｛`; parameter di sisi kiri → dipakai badan di sisi kanan ketika fungsi dipanggil atau pola cocok.
Baris sumber 350: Mendefinisikan fungsi atau pemetaan `input.addEventListener(”change”, () → ｛`; parameter di sisi kiri → dipakai badan di sisi kanan ketika fungsi dipanggil atau pola cocok.
Baris sumber 351: Memeriksa kondisi `!input.checked) return;`; blok terkait hanya diproses bila kondisi benar sehingga jalur ini mengikuti keadaan data atau interaksi pengguna.
Baris sumber 352: Melakukan operasi dengan memanggil `applyModePreset(input.value)` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 353: Melakukan operasi dengan memanggil `refreshConfig()` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 354: Menutup blok atau objek yang sedang disusun; tanda titik koma mengakhiri pernyataan.
Baris sumber 355: Menutup blok atau objek yang sedang disusun; tanda titik koma mengakhiri pernyataan.
Baris sumber 357: Melakukan operasi dengan memanggil `refreshConfig()` dan menggunakan hasilnya pada operasi ini; argumen memasok data yang dibutuhkan fungsi.
Baris sumber 358: Melakukan operasi dengan membaca nilai `｝)()` dari variabel, properti, atau ekspresi yang telah disiapkan sebelumnya.
Baris sumber 359: Menutup elemen `script` (skrip browser atau referensi berkas JavaScript) agar struktur anak berikutnya kembali berada pada elemen induk yang sesuai.
Baris sumber 360: Menutup blok atau objek yang sedang disusun sehingga ekspresi atau struktur induk dapat dilanjutkan.
*@
@model Cashflowpoly.Ui.Models.CreateRulesetViewModel

@{
    var isEditMode = Model.IsEditMode && Model.RulesetId.HasValue;
    var formActionUrl = isEditMode
        ? Url.Action("Edit", "Rulesets", new { rulesetId = Model.RulesetId })
        : Url.Action("Create", "Rulesets");
    ViewData["Title"] = isEditMode ? Context.T("rulesets.edit_title") : Context.T("rulesets.create");
}

<section class="section-shell">
    <div class="action-toolbar mb-4">
        @if (isEditMode)
        {
            <a class="btn-ghost"
               asp-controller="Rulesets"
               asp-action="Details"
               asp-route-rulesetId="@Model.RulesetId">@("<- " + Context.T("rulesets.back_to_list"))</a>
        }
        else
        {
            <a class="btn-ghost" asp-controller="Rulesets" asp-action="Index">@("<- " + Context.T("rulesets.back_to_list"))</a>
        }
    </div>

    <div class="page-intro">
        <div class="page-intro-main">
            <p class="eyebrow">@Context.T("rulesets.management")</p>
            <h1 class="headline">@(isEditMode ? Context.T("rulesets.edit_title") : Context.T("rulesets.create_title"))</h1>
            <p class="subhead">@(isEditMode ? Context.T("rulesets.edit_subtitle") : Context.T("rulesets.create_subtitle"))</p>
        </div>
    </div>

    @if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
    {
        <div class="mt-4 alert alert-error" aria-live="polite">
            @Model.ErrorMessage
        </div>
    }

    <div class="mt-6 ruleset-unified">
        <section class="ruleset-form-section">
            <details class="mt-3 data-toggle">
                <summary>@Context.T("rulesets.tips.form_title")</summary>
                <div class="mt-3 metric-panel">
                    <div class="metric-list">
                        <div class="metric-row"><span class="metric-key">@Context.T("sessions.step") 1</span><span class="metric-value">@Context.T("rulesets.tip.versioning_draft")</span></div>
                        <div class="metric-row"><span class="metric-key">@Context.T("sessions.step") 2</span><span class="metric-value">@Context.T("rulesets.tip.consistency")</span></div>
                        <div class="metric-row"><span class="metric-key">@Context.T("sessions.step") 3</span><span class="metric-value">@Context.T("rulesets.tip.safety_before_start")</span></div>
                    </div>
                </div>
            </details>
        </section>

        <form class="mt-4 form-grid ruleset-create-form" method="post" action="@formActionUrl" data-ruleset-create-form data-ruleset-mode="pemula">
            @Html.AntiForgeryToken()
            <div class="form-field">
                <label class="form-label" for="ruleset-name">@Context.T("common.name")</label>
                <input class="form-input"
                       id="ruleset-name"
                       asp-for="Name"
                       required />
            </div>
            <div class="form-field">
                <label class="form-label" for="ruleset-description">@Context.T("common.description")</label>
                <input class="form-input"
                       id="ruleset-description"
                       asp-for="Description"
                       placeholder="@Context.T("rulesets.form.description_placeholder")" />
                <p class="mt-1 text-xs text-slate-500">@Context.T("rulesets.form.description_hint")</p>
            </div>

            <div class="ruleset-form-section">
                <div class="ruleset-section-head">
                    <p class="ruleset-section-title">@Context.T("rulesets.form.core_setup")</p>
                    <p class="ruleset-section-subtitle">@Context.T("rulesets.form.help.core_setup")</p>
                </div>
                <div class="grid gap-3 md:grid-cols-2 mt-3">
                    <fieldset class="md:col-span-2">
                        <legend class="sr-only">@Context.T("rulesets.form.mode")</legend>
                        <div class="ruleset-section-head">
                            <p class="ruleset-section-title">@Context.T("rulesets.form.mode")</p>
                            <p class="ruleset-section-subtitle ruleset-mode-picker-hint">@Context.T("rulesets.form.mode_choice_hint")</p>
                        </div>
                        <div class="ruleset-mode-picker mt-2">
                            <label class="ruleset-mode-option ruleset-mode-option--beginner">
                                <input class="ruleset-mode-radio" type="radio" name="cfg-mode" value="PEMULA" checked />
                                <span class="ruleset-mode-option-content">
                                    <span class="ruleset-mode-badge ruleset-mode-badge--beginner">@Context.T("rulesets.form.mode_beginner")</span>
                                    <span class="ruleset-mode-option-copy">@Context.T("rulesets.quick_guide_beginner")</span>
                                </span>
                            </label>
                            <label class="ruleset-mode-option ruleset-mode-option--advanced">
                                <input class="ruleset-mode-radio" type="radio" name="cfg-mode" value="MAHIR" />
                                <span class="ruleset-mode-option-content">
                                    <span class="ruleset-mode-badge ruleset-mode-badge--advanced">@Context.T("rulesets.form.mode_advanced")</span>
                                    <span class="ruleset-mode-option-copy">@Context.T("rulesets.quick_guide_advanced")</span>
                                </span>
                            </label>
                        </div>
                    </fieldset>
                    <div>
                        <label class="form-label" for="cfg-actions">@Context.T("rulesets.form.actions_per_turn")</label>
                        <input id="cfg-actions" type="number" min="1" class="form-input" value="2" />
                    </div>
                    <div>
                        <label class="form-label" for="cfg-cash">@Context.T("rulesets.form.starting_cash")</label>
                        <input id="cfg-cash" type="number" min="0" class="form-input" value="20" />
                    </div>
                    <div class="md:col-span-2">
                        <label class="form-label" for="cfg-player-ordering-display">@Context.T("rulesets.form.player_ordering")</label>
                        <input id="cfg-player-ordering-display"
                               type="text"
                               readonly
                               class="form-input"
                               value="@Context.T("rulesets.form.player_ordering_player_order")" />
                        <input id="cfg-player-ordering" type="hidden" value="PLAYER_ORDER" />
                        <p class="mt-1 text-xs text-slate-500">@Context.T("rulesets.form.player_ordering_fixed_hint")</p>
                    </div>
                    <div>
                        <label class="form-label" for="cfg-freelance-income">@Context.T("rulesets.form.freelance_income")</label>
                        <input id="cfg-freelance-income" type="number" min="0" class="form-input" value="1" />
                    </div>
                </div>
            </div>

            <div class="ruleset-form-section">
                <div class="ruleset-section-head">
                    <p class="ruleset-section-title">@Context.T("rulesets.form.weekday_features")</p>
                    <p class="ruleset-section-subtitle">@Context.T("rulesets.form.help.weekday_features")</p>
                </div>
                <div class="grid gap-3 md:grid-cols-3 mt-3">
                    <label class="metric-row"><span class="metric-key">@Context.T("rulesets.form.friday_donation")</span><input id="cfg-friday" type="checkbox" checked /></label>
                    <label class="metric-row"><span class="metric-key">@Context.T("rulesets.form.saturday_gold_trade")</span><input id="cfg-saturday" type="checkbox" checked /></label>
                    <label class="metric-row"><span class="metric-key">@Context.T("rulesets.form.sunday_rest")</span><input id="cfg-sunday" type="checkbox" checked /></label>
                </div>
            </div>

            <div class="ruleset-form-section">
                <div class="ruleset-section-head">
                    <p class="ruleset-section-title">@Context.T("rulesets.form.constraints")</p>
                    <p class="ruleset-section-subtitle">@Context.T("rulesets.form.help.constraints")</p>
                </div>
                <div class="grid gap-3 md:grid-cols-2 mt-3">
                    <div>
                        <label class="form-label" for="cfg-cash-min">@Context.T("rulesets.form.cash_min")</label>
                        <input id="cfg-cash-min" type="number" class="form-input" value="0" />
                    </div>
                    <div>
                        <label class="form-label" for="cfg-max-ing-total">@Context.T("rulesets.form.max_ingredient_total")</label>
                        <input id="cfg-max-ing-total" type="number" min="1" class="form-input" value="6" />
                    </div>
                    <div>
                        <label class="form-label" for="cfg-max-same-ing">@Context.T("rulesets.form.max_same_ingredient")</label>
                        <input id="cfg-max-same-ing" type="number" min="1" class="form-input" value="3" />
                    </div>
                </div>
            </div>

            <div class="ruleset-form-section">
                <div class="ruleset-section-head">
                    <p class="ruleset-section-title">@Context.T("rulesets.form.economy_and_donation")</p>
                    <p class="ruleset-section-subtitle">@Context.T("rulesets.form.help.economy_and_donation")</p>
                </div>
                <div class="grid gap-3 md:grid-cols-2 mt-3">
                    <div>
                        <label class="form-label" for="cfg-donation-min">@Context.T("rulesets.form.donation_min")</label>
                        <input id="cfg-donation-min" type="number" min="1" class="form-input" value="1" />
                    </div>
                    <div>
                        <label class="form-label" for="cfg-donation-max">@Context.T("rulesets.form.donation_max")</label>
                        <input id="cfg-donation-max" type="number" min="1" class="form-input" value="999999" />
                    </div>
                    <label class="metric-row"><span class="metric-key">@Context.T("rulesets.form.gold_buy")</span><input id="cfg-gold-buy" type="checkbox" checked /></label>
                    <label class="metric-row"><span class="metric-key">@Context.T("rulesets.form.gold_sell")</span><input id="cfg-gold-sell" type="checkbox" checked /></label>
                    <div class="ruleset-advanced-feature-grid md:col-span-2" data-ruleset-advanced-features hidden>
                        <div class="ruleset-advanced-feature-head">
                            <span class="ruleset-mode-badge ruleset-mode-badge--advanced">@Context.T("rulesets.form.mode_advanced")</span>
                            <p>@Context.T("rulesets.form.advanced_features_hint")</p>
                        </div>
                        <div class="grid gap-3 md:grid-cols-3 mt-3">
                            <label class="metric-row"><span class="metric-key">@Context.T("rulesets.form.loan_enabled")</span><input id="cfg-adv-loan" type="checkbox" disabled /></label>
                            <label class="metric-row"><span class="metric-key">@Context.T("rulesets.form.insurance_enabled")</span><input id="cfg-adv-insurance" type="checkbox" disabled /></label>
                            <label class="metric-row"><span class="metric-key">@Context.T("rulesets.form.saving_goal_enabled")</span><input id="cfg-adv-saving-goal" type="checkbox" disabled /></label>
                        </div>
                    </div>
                </div>
            </div>

            <textarea asp-for="DefinitionJson" class="hidden"></textarea>
            <div class="form-actions">
                <button class="btn-primary"
                        type="submit">@(isEditMode ? Context.T("rulesets.update") : Context.T("rulesets.save"))</button>
                @if (isEditMode)
                {
                    <a class="text-link"
                       asp-controller="Rulesets"
                       asp-action="Details"
                       asp-route-rulesetId="@Model.RulesetId">@Context.T("common.cancel")</a>
                }
                else
                {
                    <a class="text-link" asp-controller="Rulesets" asp-action="Index">@Context.T("common.cancel")</a>
                }
            </div>
        </form>
    </div>
</section>

@section Scripts {
    <script>
        (() => {
            const getEl = (id) => document.getElementById(id);
            const toInt = (id, fallback = 0) => {
                const v = parseInt(getEl(id)?.value ?? "", 10);
                return Number.isFinite(v) ? v : fallback;
            };
            const toBool = (id) => !!getEl(id)?.checked;
            const configTextArea = getEl("DefinitionJson");
            const formEl = document.querySelector("[data-ruleset-create-form]");
            const modeInputs = Array.from(document.querySelectorAll('input[name="cfg-mode"]'));
            const getMode = () => modeInputs.find((input) => input.checked)?.value ?? "PEMULA";
            const advancedFeaturePanel = document.querySelector("[data-ruleset-advanced-features]");
            const advancedFeatureInputs = [getEl("cfg-adv-loan"), getEl("cfg-adv-insurance"), getEl("cfg-adv-saving-goal")];
            const modePresets = {
                PEMULA: {
                    starting_cash: 20,
                    loan_enabled: false,
                    insurance_enabled: false,
                    saving_goal_enabled: false
                },
                MAHIR: {
                    starting_cash: 10,
                    loan_enabled: true,
                    insurance_enabled: true,
                    saving_goal_enabled: true
                }
            };

            const scoreDefaults = {
                donation_rank_points: [{ rank: 1, points: 7 }, { rank: 2, points: 5 }, { rank: 3, points: 2 }],
                gold_points_by_qty: [{ qty: 1, points: 3 }, { qty: 2, points: 5 }, { qty: 3, points: 8 }, { qty: 4, points: 12 }],
                pension_rank_points: [{ rank: 1, points: 5 }, { rank: 2, points: 3 }, { rank: 3, points: 1 }]
            };

            const readForm = () => ({
                mode: getMode(),
                actions_per_turn: toInt("cfg-actions", 2),
                starting_cash: toInt("cfg-cash", 20),
                player_ordering: "PLAYER_ORDER",
                weekday_rules: {
                    friday: { feature: "DONATION", enabled: toBool("cfg-friday") },
                    saturday: { feature: "GOLD_TRADE", enabled: toBool("cfg-saturday") },
                    sunday: { feature: "REST", enabled: toBool("cfg-sunday") }
                },
                constraints: {
                    cash_min: toInt("cfg-cash-min", 0),
                    max_ingredient_total: toInt("cfg-max-ing-total", 6),
                    max_same_ingredient: toInt("cfg-max-same-ing", 3),
                    primary_need_max_per_day: null,
                    require_primary_before_others: true
                },
                donation: {
                    min_amount: toInt("cfg-donation-min", 1),
                    max_amount: toInt("cfg-donation-max", 999999)
                },
                gold_trade: {
                    allow_buy: toBool("cfg-gold-buy"),
                    allow_sell: toBool("cfg-gold-sell")
                },
                advanced: {
                    loan: { enabled: getMode() === "MAHIR" && toBool("cfg-adv-loan") },
                    insurance: { enabled: getMode() === "MAHIR" && toBool("cfg-adv-insurance") },
                    saving_goal: { enabled: getMode() === "MAHIR" && toBool("cfg-adv-saving-goal") }
                },
                freelance: { income: toInt("cfg-freelance-income", 1) },
                scoring: scoreDefaults
            });

            const writeFormFromConfig = (cfg) => {
                if (!cfg || typeof cfg !== "object") return;
                const resolvedMode = `${cfg.mode ?? "PEMULA"}`.toUpperCase();
                modeInputs.forEach((input) => {
                    input.checked = input.value === resolvedMode;
                });
                getEl("cfg-actions").value = cfg.actions_per_turn ?? 2;
                getEl("cfg-cash").value = cfg.starting_cash ?? 20;
                getEl("cfg-freelance-income").value = cfg.freelance?.income ?? 1;
                getEl("cfg-friday").checked = !!cfg.weekday_rules?.friday?.enabled;
                getEl("cfg-saturday").checked = !!cfg.weekday_rules?.saturday?.enabled;
                getEl("cfg-sunday").checked = !!cfg.weekday_rules?.sunday?.enabled;
                getEl("cfg-cash-min").value = cfg.constraints?.cash_min ?? 0;
                getEl("cfg-max-ing-total").value = cfg.constraints?.max_ingredient_total ?? 6;
                getEl("cfg-max-same-ing").value = cfg.constraints?.max_same_ingredient ?? 3;
                getEl("cfg-donation-min").value = cfg.donation?.min_amount ?? 1;
                getEl("cfg-donation-max").value = cfg.donation?.max_amount ?? 999999;
                getEl("cfg-gold-buy").checked = cfg.gold_trade?.allow_buy ?? true;
                getEl("cfg-gold-sell").checked = cfg.gold_trade?.allow_sell ?? true;
                getEl("cfg-adv-loan").checked = !!cfg.advanced?.loan?.enabled;
                getEl("cfg-adv-insurance").checked = !!cfg.advanced?.insurance?.enabled;
                getEl("cfg-adv-saving-goal").checked = !!cfg.advanced?.saving_goal?.enabled;
                setAdvancedFeatureAvailability(resolvedMode);
            };

            const setAdvancedFeatureAvailability = (mode) => {
                const isAdvanced = `${mode ?? ""}`.toUpperCase() === "MAHIR";
                if (advancedFeaturePanel) {
                    advancedFeaturePanel.hidden = !isAdvanced;
                    advancedFeaturePanel.setAttribute("aria-hidden", `${!isAdvanced}`);
                }
                advancedFeatureInputs.forEach((input) => {
                    if (!input) return;
                    input.disabled = !isAdvanced;
                    if (!isAdvanced) input.checked = false;
                });
            };

            const applyModePreset = (mode) => {
                const preset = modePresets[`${mode ?? ""}`.toUpperCase()];
                if (!preset) return;
                getEl("cfg-cash").value = preset.starting_cash;
                getEl("cfg-adv-loan").checked = preset.loan_enabled;
                getEl("cfg-adv-insurance").checked = preset.insurance_enabled;
                getEl("cfg-adv-saving-goal").checked = preset.saving_goal_enabled;
                setAdvancedFeatureAvailability(mode);
            };

            const refreshConfig = () => {
                const cfg = readForm();
                if (formEl) formEl.dataset.rulesetMode = cfg.mode.toLowerCase();
                const json = JSON.stringify(cfg, null, 2);
                configTextArea.value = json;
            };

            try {
                if (configTextArea?.value?.trim()) {
                    writeFormFromConfig(JSON.parse(configTextArea.value));
                }
            } catch (_) {}

            setAdvancedFeatureAvailability(getMode());

            document.querySelectorAll("input, select").forEach((el) => {
                el.addEventListener("input", refreshConfig);
                el.addEventListener("change", refreshConfig);
            });

            modeInputs.forEach((input) => {
                input.addEventListener("change", () => {
                    if (!input.checked) return;
                    applyModePreset(input.value);
                    refreshConfig();
                });
            });

            refreshConfig();
        })();
    </script>
}
